[pytest]
testpaths = tests persistent_agents/tests
markers =
    integration: marks tests that require a running backend (deselect with '-m "not integration"')
    asyncio: marks async tests
    perf: marks performance probes with wall-clock assertions (run explicitly)
asyncio_mode = auto
