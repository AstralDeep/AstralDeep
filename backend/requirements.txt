# Core
# First-party Projection, Plane, Primitives, and LETS distributions are built
# and installed from the exact initialized submodules by
# scripts/install_local_components.py. Keep them out of this resolver input so
# an index package can never substitute for the composition's local wheel.
websockets>=12.0
openai>=1.0.0
python-dotenv>=1.0.0
aiohttp>=3.9.0
fastapi>=0.100.0
uvicorn
python-multipart>=0.0.6
psutil>=5.9.0
psycopg2-binary>=2.9.0
requests>=2.31.0
plotly>=5.18.0

# Data processing
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.0

# File-upload parsing (feature 002-file-uploads, lead-developer approved)
pypdf>=4.0.0
python-docx>=1.0.0
python-pptx>=0.6.23
odfpy>=1.4.1
striprtf>=0.0.26
Pillow>=10.0.0
pdf2image>=1.17.0
defusedxml>=0.7.1
python-magic>=0.4.27; sys_platform != "win32"
python-magic-bin>=0.4.14; sys_platform == "win32"

# Medical imaging (feature: medical-file-uploads)
numpy>=1.26
pydicom>=3.0.0
nibabel>=5.2.0
tifffile>=2024.8.10
aicspylibczi>=3.1.0
SimpleITK>=2.3.0
openslide-bin>=4.0.0.6  # bundles native libopenslide — no separate install needed
openslide-python>=1.3.0

# PHI detection (feature 025-agentic-soul-integration, lead-developer approved 2026-05-27)
# Runs locally in-process (no PHI egress); gates durable agent memory so no PHI persists.
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0
# NOTE: the spaCy model `en_core_web_lg` is downloaded at image build time (see Dockerfile),
# not at runtime, to avoid any network fetch in production.

# Timezone database for tz-aware scheduled jobs (feature 025, FR-020).
# zoneinfo (stdlib) needs the IANA tz DB, which slim base images lack.
# NOTE: requires the same lead-developer dependency approval as above.
tzdata>=2024.1

# A2A + MCP
a2a-sdk[sqlite]>=1.0.2,<2.0
a2a-sdk[http-server]>=1.0.2,<2.0
httpx>=0.28.1

# Conversational voice control plane (feature 065; lead-developer approved 2026-07-31).
# Orchestrator-only room/token administration. Never install this package or the
# LiveKit API key/secret in the direct-RTC media worker.
livekit-api==1.2.0

# Remote compute agents (feature 063-remote-compute-agents, lead-developer approved D1 2026-07-23).
# SSH transport for cluster (Slurm) + host operations. Pulls bcrypt, pynacl, cffi transitively.
paramiko>=3.4.0
# cryptography is used directly (credential_manager Fernet) and by paramiko/python-jose;
# pin it explicitly here rather than relying on transitive resolution.
cryptography>=42.0.0
# LETS protected-executor signature verification is a direct runtime use. The
# local-component installer is intentionally --no-deps, so do not rely on the
# LETS wheel or Paramiko to resolve this transitively.
PyNaCl>=1.6.2,<2

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
arxiv
reportlab
python-jose[cryptography]
scipy
Jinja2
