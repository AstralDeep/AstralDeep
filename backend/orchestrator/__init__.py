"""Marks backend/orchestrator as a Python package; carries no code of its own. Imported
implicitly by every module under orchestrator/ and everything that reaches into the
orchestrator subsystem.
"""
