"""Marks the scheduler package: a durable job store, a timezone-aware
cron/interval/one-shot evaluator, an asyncio dispatch loop, and a runner that
executes due jobs under fresh, scope-bounded delegated authority.
"""
