"""Marks the remote_control package: the mutating-tier remote-compute verb library
(mcp_tools.py) unioned into agents.remote_compute, with every destructive verb gated
by orchestrator/remote_confirmation.py.
"""
