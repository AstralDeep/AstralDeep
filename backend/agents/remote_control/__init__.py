"""remote-control-1 — mutating remote-compute agent (feature 063). NEVER safe-seeded;
every verb needs an explicit per-user grant, and destructive verbs route through the
US3 confirmation gate before they can act."""
