# Native UI v2 verification

## Source and toolchain

Baseline Deep `341f58c5625d7dda530720e4cac6b3d35ae47492`, embedded Projection `7cb7e25483e023df68f3c6a762ef87b2e36417fb`. The baseline preceded `codex/090-native-ui-v2`; the September25 collaboration checkpoint is published in both repositories (details below). Owner confirmed updates ready on September 24, 2026. Docker 29.8.0; Xcode 27.0 (27A266a), Swift 6.4; available iOS/watchOS 26.5 simulators. Local Xcode differs from hosted CI's pin.

## Current backend and browser

- `make composition-preflight PYTHON=python3`: passed, composition digest `0c060a74e6e8c9d761a1e193d0923c84e4c83227bb835a01884cb36338232a55`.
- Fresh ARM image build initially failed for existing aicspylibczi due to missing OpenSSL development libraries. Added `libssl-dev` to Dockerfile build prerequisites. `docker build -t astraldeep:ui-v2-reference .`: passed. Image `sha256:e5223ebf731f68d4f4b06c284e92385dc575b27396a62a7e7a7a2f9868625048` contains baseline UI source plus this build-prerequisite change.
- `docker compose -f docker-compose.yml -f /tmp/astral-native-ui-v2-compose.yml up -d --no-build astraldeep`: passed. Temporary override selects the reference image. Backend, PostgreSQL and LiveKit running; backend/PostgreSQL healthy.
- `/healthz`: OK; `/readyz`: OK, database/publication OK, ten agents, local LETS disabled by existing configuration.
- `docker exec astraldeep python scripts/install_local_components.py verify --root /app --lock /opt/astral-component-wheels/astral-component-wheels.lock.json`: four components verified.
- Served/source SHA256 equality: client.js `0b1931672e4e38f6552ea58396cb3734f65a4e2d566127a0b2bc6c4c33ece706`; astral.css `089ccbe370dcaeed91fd9caf104015e603667bb21968a335f185199164c44615`.
- Owner completed normal web Keycloak sign-in. Landing/settings captures inspected at seven required sizes; phone drawer, composer More and desktop agent intro/error also captured. Existing History collapsed for screenshots. Assets and hashes live in kos-wiki `assets/astral-native-ui-v2/web-reference/`.
- Ordinary dice scenario dispatch reached the configured upstream model, which returned HTTP 404 `model_not_found`. Error reference is real; successful result/fullscreen references remain pending owner model configuration. No credentials were read or changed.
- Voice worker was not yet running during initial captures. The owner explicitly excludes the bottom web voice warning from native UI; native worker connection/recording/playback/recovery remain required.

## Unchanged Apple baseline

- `swift test --package-path components/AstralProjection/apple-clients/AstralCore --enable-code-coverage`: 254 tests, five failed assertions. ManifestDriftTests expects 35 components/136 actions versus current 41/138; PrimitivesTests lacks six v2 mirror types; WatchPresentation088Tests expects a weather emoji while current fixture uses `WX`. These are baseline failures to resolve, not accepted waivers.
- `xcodebuild -project components/AstralProjection/apple-clients/AstralApp/AstralApp.xcodeproj -scheme AstralApp -destination 'platform=macOS' -derivedDataPath /tmp/astral-native-ui-v2-macos -configuration Debug CODE_SIGNING_ALLOWED=NO build`: BUILD SUCCEEDED before native changes.

## Remaining qualification

Shared contract, native implementation, changed coverage, CI-equivalent suites, native signed-in visual comparison and live voice are pending. No passing hosted CI, final parity, release or deployment claim.


## September 25 complete references and published collaboration checkpoint

- All54 planned screenshots are visually inspected, their PNG dimensions/SHA256 verified, and the complete7-state ×7-size matrix plus drawer/error references pushed to kos-wiki `92e4ed0c8254df10f0fc196c0aa8a11fb432ecae`. Remote main was independently queried and matches. Native UI edits paused for this prerequisite and resumed only after the verified push.
- Main and system models persist as `zai-org/GLM-5.3-Flash` after reopening normal product settings. Real dice dispatch completed with model200, delegated authorization, roll_dice success and final model200; six rolls5,3,3,2,2,1 total16 supply the result references. Ordinary reload refreshed an expired IAM session that had produced skill_lookup_unavailable; no bypass or provider secret handling.
- Voice worker build/start and exact ASR/TTS preflight pass; authenticated voice status reports ready. Native recording/playback/recovery are not yet verified.
- `xcodebuild -project components/AstralProjection/apple-clients/AstralApp/AstralApp.xcodeproj -scheme AstralApp -destination 'platform=macOS' -derivedDataPath /tmp/astral-native-ui-v2-macos -configuration Debug CODE_SIGNING_ALLOWED=NO build`: BUILD SUCCEEDED with partial console shell/composer/result implementation; log `/tmp/astral-native-ui-v2-console-build.log`.
- Changed Python source/tests: Ruff check passed. Changed Swift files:38 formatted and strict-linted with the temporary configuration merging Xcode27 required defaults; the repository's hosted-toolchain formatting gate still requires final qualification.
- Earlier verification:52 real-browser tests passed; Core278 passed before watch-preparation changes;29 macOS primitive/export/work-renderer tests passed, export changed-line coverage96.43%. Final palette/sizing changes and current watch-preparation/core changes require reruns. Root console shell/state tests and final changed-line coverage remain open.
- Shared targeted watch/presentation/menu tests264 passed; watch/guidance/console-label tests87 passed. Six earlier Deep session-authority fixture failures pass in isolation; latest combined real-PostgreSQL suite is being rerun. Do not treat prior combined failure as waived.
- `handoff-windows.md` records the owner's new branch-publishing request. Product publication is a work-in-progress collaboration checkpoint, not final CI, parity, merge, deployment or release. Windows source remains unchanged; watch client negotiation remains disabled until its implementation is ready.

- Publication-freeze follow-up: full AstralCore `swift test --package-path apple-clients/AstralCore --scratch-path /tmp/astral-core-console-v2 --enable-code-coverage` passed278/278 after truthful default-network fixture corrections, log `/tmp/astral-core-watch-freeze.log`. Changed browser helper/spec files pass ESLint with zero warnings.
- Combined Deep shared-surface suite completed152/152 against disposable PostgreSQL17, log `/tmp/astral-watch-surfaces-tests.log`; changed executable lines in native_console/guidance/agent_intro/ChromeSurface are41/41 covered. Two newest watch PostgreSQL cases were added after that collection and remain to run. The previous six failures are resolved by passing rerun without weakening authorization.


## September25 Apple qualification after publication

- Independently reverified published Deep4e2eae707e108ee868212cec653ae107fdb15518 and Projection1d4a8642a888279dd42a67181412901960b50217. New work remains local after those shared Windows handoff anchors.
- Built the exact published checkpoint in detached worktree `/tmp/astral-native-ui-v2-build` with all four pinned submodules. `docker build -t astraldeep:native-ui-v2-090 .` passed; image `sha256:4347b3e1287349e3b4895a2be7879eb1ce7edb4275d7b7a1c495c0252fa3f209`. Normal Compose with `/tmp/astral-native-ui-v2-active-compose.yml` recreated the backend; health is healthy and worker remains running.
- The new Mac app reused its existing normal authenticated session and displayed the server-owned console/catalog. Initial live inspection and review identified introduction Run attachment/modal behavior, phone sign-out, selection disclosure, restored/voice conversation visibility and preview clipping gaps. Fixes and view regressions are underway; this is not live-parity acceptance.
- `xcodebuild -project apple-clients/AstralApp/AstralApp.xcodeproj -scheme AstralApp -destination 'platform=macOS' -derivedDataPath /tmp/astral-console-interaction -configuration Debug CODE_SIGNING_ALLOWED=NO -parallel-testing-enabled NO -only-testing:AstralAppTests/ConsoleInteractionTests -only-testing:AstralAppTests/ConversationContinuityTests -only-testing:AstralAppTests/VoiceSessionController065Tests -resultBundlePath /tmp/astral-console-interaction-qualified.xcresult test`:73tests passed,0failures. Includes exact-current Load/Run, ready attachments, confirmed selections/reset, actual aggregate update at unchanged dimensions, private-surface account cleanup, restore visibility and current-connection voice dispatch. Log `/tmp/astral-console-interaction-qualified.log`. Voice controller fixtures do not prove real microphone/worker media playback.
- `PYTHONPATH=backend:src .venv/bin/python -m pytest tests/rote tests/test_disposition_matrix_088.py tests/test_guidance_protocol_088.py tests/test_work_protocol_088.py tests/chrome/test_console_model.py -q` from Projection using the root Python environment:1271passed after the additive manifest inventory. The earlier three capability-declaration mismatches are resolved. Log `/tmp/astral-console-manifest-full.log`.
- Component renderer/export qualification:32macOS and31iOS tests passed after the stat-cell stretch fix; measured changed-line coverage ComponentView93.05%macOS/93.62%iOS and CanvasCapture96.43%. Canonical coverage exports and copied xcresults are under ignored `components/AstralProjection/build/090-component-qualification/`.
- New bundled canonical console fixtures avoid native test-host filesystem access and support simulator tests without duplicate fixtures. Watch implementation/tests, root rendered shell coverage, native visual/voice acceptance, Android work, composition repin/rebuild and complete CI remain open.


## September 25 native voice completion and conversation delivery

- The guidance row-lock fix retries only PostgreSQL lock-not-available conflicts before a protected catalog callback begins, with 50/100/150 ms bounded delays and full authority revalidation. Real PostgreSQL release/revocation/persistent-lock/cancellation tests pass. Combined guidance qualification: 53 passed in 240.11 seconds (`/tmp/astral-native-voice-lock-qualified.log`); changed executable lines in `turn_guidance_authority.py`: 22/24 covered (91.67%).
- Live backend image is `sha256:95d4ec0e151036eb49c0108c38b609bd5b266a02785248279aedf21dddec1c6a`, built from the isolated published checkout plus the documented guidance diagnostics/retry and agent-intro patches. This is not yet the final whole-tree image or final composition pin.
- After spoken voice succeeded, the owner reported no visual response. A newly created native voice chat had not been loaded through the server conversation path; its socket therefore lacked the result-delivery scope. Apple now sends the ordinary authenticated `load_chat`, waits for its accepted matching hydration snapshot, then starts voice. Timeout, End, chat switch and connection replacement prevent stale activation. Existing chats also refresh before activation.
- Regression suite: macOS75/75 and iOS69/69 passed (voice controller, recovery and conversation continuity), including model-level new-chat load ordering and committed response/canvas rendering. Results: `/tmp/astral-native-voice-hydration-qualified.xcresult` and `/tmp/astral-native-voice-hydration-ios.xcresult`. Unsigned Mac build passed.
- **Live owner confirmation:** in the rebuilt Mac app, a fresh “Roll six dice” request displayed response text and dice UI and played the spoken reply. Microphone activation was user-operated. No ambient transcript or private history is archived. This closes the reported Mac request/delivery failures, not the entire native parity or CI matrix.
- Intro rendering qualified separately: Mac8/8 and iOS5/5 tests pass, including desktop/phone geometry, large text, and real Load/Run interactions. Watch equivalent conversation hydration, remaining Apple live form factors, Android implementation, source provenance and full CI remain in progress.


## September 25 Apple implementation / available verification checkpoint

Apple phone/tablet/Mac and watch presentation implementations are present; the complete web reference archive predates resumed UI changes. Available verification now includes Mac292/292 app tests, iOS69/69 voice/continuity tests, watch105tests with0failures/1existing release-evidence skip, and Core283 previously qualified. The shell fixtures are mounted through the real RootView; its8Mac rendered/interaction tests pass. Watch now uses ordinary chat hydration before voice activation, with timeout/background/chat-switch/connection cancellation checks. Source/export parity29tests pass with1immutable-source replay skip (separate source-repo input required). Native export resources were refreshed from the canonical generated document, and a mirror check now prevents silent Apple bundle drift.

The owner confirms real Mac microphone capture, spoken successful dice response, and matching text/canvas. iPhone17Pro is installed at normal SSO in Xcode27 Device Hub; owner-operated sign-in is pending. iPad/watch live sign-in and media remain open. A local changed-line diagnostic confirms AppModel91.67%, ComponentView94.26%, CanvasCapture96.43%, Intro100%, Shell92.82%, chart100%; other UI, voice and watch paths still need coverage expansion, so this is not final coverage/CI completion.

This records the Apple implementation and available verification boundary required before Android work. T016/T029 remain unchecked until their remaining live and qualification acceptance is complete; Android adaptation starts after this checkpoint while user-operated Apple sign-in remains pending. No release or all-client CI claim is made.


## September 25 macOS composer correction

The owner reported vertically misaligned attachment/More icons and an oversized gap before Send. The custom controls layout incorrectly reserved320points when asked for its ideal size and anchored the final control at that width. It now measures its actual intrinsic width, centers each row, and keeps6point gaps; native menus use plain button styling with44point targets, and the message row centers the controls. The two-row narrow fallback aligns the group to the trailing edge.

Unsigned Mac build passed and the rebuilt live app visibly aligns attachment/microphone/More/Send. Both menus opened with their normal actions. A rendered pixel regression checks the actual glyph bounds at320/390/768/1440points; console presentation9, interaction10 and voice controller59tests pass (78total) in `/tmp/astral-macos-composer-qualified.xcresult`. Changed Swift files pass strict formatting. Four synthetic native captures are archived in kos-wiki `assets/astral-native-ui-v2/native-composer/`. This addresses the reported Mac composer defect; remaining Apple live/coverage and fullCI gates remain open.


## September 25 Android contract and renderer foundation

Android now decodes bounded console catalog/presentation/selection contracts, updates the app's ROTE geometry, fences private selection confirmation to its current connection and request, retains drafts through geometry changes, and forwards the explicit selection with ordinary chat. Scenario/intro Load and Run actions require the current server offer. Legacy welcome nodes no longer compete with the console workspace. Console negotiation is not yet enabled; the actual console shell, capability observer and voice hydration remain implementation work.

Six missing renderers now have normalized native presentations: action_group, stat_group, gauge, pipeline_stepper, donut_chart and radar_chart. Their tests include malformed values, overflow, disabled payload identity and accessible semantic states. Exports omit action payloads and use captured visible pixels for the five visual composites. Core160tests and app375unit tests pass; all four new core presentation/selection files have100%line coverage (339executable lines combined), and core Kover verification passes. Evidence: `/tmp/astral-android-composite-unit-qualified.log`, core Kover XML and current JUnit XML. Native emulator rendering/export tests are being qualified separately; these unit results do not establish live Android parity or voice acceptance.

The Mac composer fix also passes the unsigned iOS build (`/tmp/astral-composer-ios-build.log`). Product publication anchors remain unchanged; all work after the published090handoff is local.


## September 25 Android native composite qualification

The Pixel7Pro Android17 emulator passes4instrumented composite tests in6.726seconds with canonical Open Sans. Tests exercise disabled action refusal and payload identity, compact/default and larger text rendering, real nonblank foreground pixels, theme updates, retained export pixels after unmount, owner reset and unavailable pixel refusal. Live test failures found and fixed the generic button renderer ignoring `disabled` and export collection before the first completed frame. Visual capture inspection found primary text inheriting black outside a Material surface; every new composite primary text now uses the theme foreground. All10corrected component captures were inspected and archived in kos-wiki `assets/astral-native-ui-v2/android-composites/`, with dimensions/hashes and exact source/APK identities. They are synthetic component evidence, not authenticated whole-client parity.

Current owning Android gates pass: `ktlintCheck :app:lintDebug :core:test :app:testDebugUnitTest :core:koverVerify :app:assembleDebug` (`/tmp/astral-android-composites-final-gates.log`,16seconds), core160/app375unit tests; instrumented result `/tmp/astral-android-composites-palette-qualified.log`. APK inspection verifies Open Sans and the unchanged authoritative license are packaged. Full app changed-line coverage, Android console/capability/voice implementation and live phone/tablet acceptance, remaining Apple live/coverage, and complete backend/shared CI remain open.


## September 25 Android voice conversation hydration

Android now creates or refreshes the selected voice chat through ordinary correlated `load_chat` delivery and waits for the exact accepted hydration snapshot before start/takeover. The live-connection transport refuses offline queuing and owner/socket replacement. End, new chat, chat switch, typed-request replacement, backgrounding, reconnect, authentication loss and sign-out fence late activation; abandoned chat creation cannot replace the visible conversation. Existing hydration retry at5seconds reports a voice retry message, and new-chat creation has a10second preparation deadline. Backgrounding also retires an in-flight voice acquisition and ends a stale returned session before connecting media.

Model/controller/transport regressions exercise new and existing chat ordering, exact snapshot/connection/request acceptance, cancellation and timeout, ordinary typed/new-chat acknowledgement continuity, offline refusal and owner retirement. Current app383unit tests and core160tests pass. The complete owning Android lint/unit/core-Kover/debug-build gate passes with all4composite instrumented tests in21seconds (`/tmp/astral-android-voice-foundation-gates.log`). This verifies implementation and controlled state/media tests; real authenticated Android microphone/worker/result delivery remains open until the console and capabilities are connected and exercised.


## September 25 Android runtime capabilities and chart resizing

The native host now measures its actual content viewport and reports current touch/pointer, network, reduced-motion and audio/permission facts. Live updates are deduplicated; offline changes collapse into the next registration rather than queued stale frames. Same-owner host remount preserves the connection and draft. Common display, orientation, density, font, keyboard and UI-mode changes recompose the retained Activity; permission responses also update voice capability before activation. Normal authentication and voice lifecycle fences remain intact.

Two real-emulator observer tests pass: normal WebSocket registration/update ordering with resize/remount continuity, and Android service/motion observation plus disposal. Chart qualification caught Plotly retaining its old drawing width despite a smaller host. Native relayout now explicitly uses the measured chart width without replacing the document or zoom; theme changes refresh its palette document. Eight portable canvas/export tests pass, including the new resize/zoom/theme regression and existing isolation/export checks.

The complete owning Android gate passes in30seconds: `ktlintCheck :app:lintDebug :core:test :app:testDebugUnitTest :core:koverVerify :app:assembleDebug`, plus14instrumented tests (observer2, portable export8, composite4). Core160/app387unit tests have0failures/skips. Evidence: `/tmp/astral-android-device-chart-qualified.log`. Console negotiation stays off until its visible shell is implemented. Actual authenticated Android voice/phone/tablet acceptance, final changed coverage, remaining Apple acceptance and backend/shared CI are still open. No new product publication.


## September 25 Android console layout checkpoint

The new native shell consumes server console/ROTE geometry and current catalog actions. Landing, sidebar/drawer search, account/settings, compact/large-text composer, current introduction Load actions and mandatory setup are exercised in the emulator. One persistent result host retains the same chart document and zoom through preview, collapse, full-screen and resize. A stronger resize assertion exposed Android view size changes that did not invoke the Compose update callback; the native view now relayouts on actual size changes. Larger-text header/composer placement, status color, modal duplicate titles and horizontal More icon were corrected after screenshot inspection.

Android lint/core Kover/debug build, core160/app388unit and17instrumented checks pass (`/tmp/astral-android-console-qualified.log`,37seconds). The expanded console suite passes5tests (`/tmp/astral-android-console-interactions.log`,13seconds; capture rerun5/5 in7.667seconds). Shared targeted contracts/ROTE pass1306tests after updating Android's implemented guidance-selection disposition (`/tmp/astral-native-current-shared-checks.log`). Eleven inspected synthetic captures are archived in kos-wiki `assets/astral-native-ui-v2/android-console/` with exact source/APK hashes. They are canonical fixture evidence, not authenticated native acceptance; host images retain emulator safe-area padding.

Console negotiation remains off at this checkpoint. Remaining visual/behavior review, final changed coverage, authenticated phone/tablet voice and remaining Apple acceptance/full backend/shared CI remain open. The user has authorized unattended continuation and is unavailable for sign-in/microphone interaction. Product remote anchors are unchanged.

## September 25 Android console negotiation and full-suite qualification

Console/v2 is now negotiated by the measured Android device observer. Authenticated history deletion validates the current offer and owner, preserves content on denial, and clears current-chat private state/resume storage only after confirmed deletion. Actual source-agent labels, status badge colors and compact composer field thresholds were corrected. Core161/app392 unit and21 instrumented checks pass with ktlintCheck, lintDebug, core Kover verification and debug assembly (`/tmp/astral-android-console-enabled-qualified.log`).

Full Projection passes3231 tests, including immutable extraction replay, after refreshing owned transformation entries without changing Windows (`/tmp/astral-090-projection-current-full-qualified.log`; coverage XML `/tmp/astral-090-projection-current-full-coverage.xml`). Browser continuity passes93 tests (`/tmp/astral-090-web-contract-current.log`), ESLint and the45 exercised coverage-tooling tests pass. Ruff passes maintained tracked/new Deep Python sources and Projection src/backend/tests/scripts. Root `ruff check .` also traversed private historical Python files under this machine's `.git`; those3032 metadata-only diagnostics are excluded from the source inventory, not rewritten.

The full114-scenario Android coverage producer currently fails3tests (`/private/tmp/astral-android-console-current-coverage/fixtures.log`): two old reconnect fixtures call same-owner start, now intentionally preserving the connection; the third assumes old welcome-button font wrapping. Repairs use physical socket close and verify wrapping/centering/no overflow under canonical Open Sans. Current reruns are pending. Watch Work navigation still has an initial-request failure under investigation; no failed aggregate is reported as passing. Full backend/module diagnostics run against disposable PostgreSQL and the dirty worktree, separate from canonical clean-candidate CI.

The Mac is locked and owner unavailable. Shell/build/emulator checks continue; native PKCE and actual microphone checks requiring the owner remain pending. No new product publication occurred.


## September 25 unattended regression repairs

The full-suite Android reconnect/font regressions are repaired and all 42 targeted tests pass (`/tmp/astral-android-full-suite-repairs.log`). Console category selection survives conversation transitions and viewport changes, retiring only when its catalog category disappears. The foreground observer restores voice readiness when an already-started host remounts; nine console/lifecycle checks pass (`/tmp/astral-android-console-lifecycle-qualified-rerun.log`). An additional real Compose test verifies five active voice controls remain within 320/390/768/1440-point layouts, including enlarged text, keyboard submission and disabled-action refusal (`/tmp/astral-android-composer-active-controls-rerun.log`). Current Android owning gates pass in 28 seconds: ktlintCheck, lintDebug, core161/app392 unit tests, core Kover and debug assembly. The dedicated coverage variant builds; the expanded 116-scenario device producer is running, so final app changed coverage remains open.

All seven watch navigation tests pass (`/tmp/astral-watch-navigation-session-fixed.xcresult`). A conditional Group in the test harness replaced the task-owning view when readiness changed, cancelling the socket session before the first Work request; a stable ZStack host preserves the session. Shipped watch navigation was not altered for this harness repair. AstralCore283 tests pass with coverage. Strict maintained Swift formatting passes using the Xcode27 configuration translation that preserves the repository's grouped imports; hosted Xcode26.6 remains separately required.

Backend diagnostics exposed worker cleanup interrupted by ASGI cancellation; the unchanged baseline reproduces intermittently in Linux too. A shielded AnyIO cancellation scope and repeated-asyncio-cancellation join now complete the exact connection release and disconnect hook before propagating cancellation. Four direct cleanup regressions include two demonstrated pre-fix failures, repeated cancellation, and propagated operation failures. Together with all endpoint/coordinator tests, 77 pass (`/tmp/astral-worker-cleanup-qualified.log`), with 12/12 changed executable endpoint lines covered (`/tmp/astral-worker-cleanup-coverage.json`). AnyIO, already a FastAPI runtime dependency, is now explicitly declared in backend requirements. The lease-expiry test waits for completed server registration before advancing its synthetic clock. Production authorization, lease duration and dispatch behavior remain unchanged. Remaining complete backend suites, fresh Mac/iOS suites and Android coverage continue; no full-CI, authenticated mobile/watch acceptance or new product publication claim is made.

## September 25 complete Android coverage and Apple/backend continuation

The dedicated Android producer now passes all116 instrumented tests with0skips in156.654seconds (`/private/tmp/astral-android-qualified-coverage/fixtures.log`). Its class-identity audit validates720class files with0mismatches. The merged report is `/private/tmp/astral-android-qualified-coverage/report/report.xml`; its source/APK identities and all11inspected current phone/tablet/desktop/large-text captures are published in kos-wiki `assets/astral-native-ui-v2/android-qualified/`. The changed-line diagnostic includes all new Kotlin sources and measures1974/2104=93.82% (`/tmp/astral-native-kotlin-changed-coverage.json`). Core161/app392, ktlintCheck, lintDebug, Kover and assembleDebug pass. These are local fixture results; exact committed-candidate and authenticated staging receipts remain open.

Fresh Mac293 and iOS277 app tests pass (`/tmp/astral-090-current-macos.xcresult`, `/tmp/astral-090-current-ios.xcresult`). Core283 passes. Current Swift union coverage before the expanded UI checks is2731/3222=84.76%, below the required90%; it is not a passing gate. The iOS timeout-label expectation was repaired and passes its rebuilt focused check (`/tmp/astral-090-ios-surface-timeout-qualified.xcresult`). A subsequent test-without-building aggregate selected stale compiled test bytes and repeated the old expectation; the full31-test selection is being rebuilt and rerun. No failed aggregate is counted as passing.

The full orchestrator module passes691tests (`/tmp/astral-090-orchestrator-current-qualified.log`), and the77worker endpoint/coordinator/cleanup tests also pass in isolated Linux (`/tmp/astral-worker-cleanup-linux-qualified.log`). The broader dirty-tree diagnostic found7persistent-agent failures in936tests and a marginal parallel-speed benchmark failure. Six persistent failures reproduce in a separate94-test selection (88pass); they arise in unchanged baseline fixture setup: patching constructors at their old import sites, omitting the explicit-notes lifecycle dependency, and omitting required observation completeness flags. Narrow fixture repairs retain production authorization and observation validation. The seventh authority refusal passed the isolated selection and remains under load-related investigation. Full backend coverage and CI qualification remain in progress.

Watch catalog Load→Send→scoped result→detail/back now works through the real navigation harness. A new More-action check exposed the handoff alert attached below the active navigation destination; moving its presenter to the shared navigation container makes it visible. Both new tests pass within the expanded run; final full-suite result and screenshot review are pending. The harness now sends ordinary connection/request/chat-scoped response frames and uses slow bounded drags; production continuity validation is unchanged.

Owner-dependent native PKCE and physical audio acceptance remain open while the owner is away. No additional product commit, push, PR, merge or release is claimed.


## September 25 current Apple navigation and changed coverage

The rebuilt full iOS UI selection passes31/31 in507.005seconds (`/tmp/astral-090-ios-ui-rebuilt.xcresult`), superseding the stale test-without-building failure. Fresh Mac app tests pass294/294 in43.344seconds (`/tmp/astral-090-macos-qualified-current.xcresult`). Current watch unit tests pass104 with one protected-staging-only skip in9.389seconds (`/tmp/astral-090-watch-qualified-current.xcresult`); the skip requires `ASTRAL_STAGING_URL` and remains an open evidence input.

All10 watch navigation UI tests pass in215.856seconds (`/tmp/astral-watch-navigation-current-all.xcresult`). Agent introduction now belongs to its directory destination; work/guidance destinations belong to their opening screen. More→Recent work→Back returns to More, then Chat. The shared navigation container presents handoff alerts above pushed screens. Test-harness theme/typography match the shipped views. Six original inspected416×496 screenshots and258source plus two executable identities are archived in kos-wiki `assets/astral-native-ui-v2/watch-qualified/`.

The consistent-current-source Swift changed-line diagnostic is2953/3231=91.40% (`/tmp/astral-090-swift-union-diagnostic.json`). It uses current Mac294/iOS277/watch104+skip, iOSUI31/watchUI10 and Core283 artifacts, including new untracked sources. Earlier watch coverage from before navigation line changes is excluded. Android remains1974/2104=93.82%. These are local numeric diagnostics, not immutable committed-candidate/protected producer receipts.

The complete backend suite is running in Linux container `astral-090-linux-ci-diagnostic`, separate from the live app/database, with hash-pinned backend CI tooling, current source mounted read-only and isolated PostgreSQL databases per module (`/tmp/astral-090-linux-current-diagnostic/`). Mac diagnostics were interrupted after platform-specific MIME/atomic-filesystem failures; no product behavior or tests were weakened for those host limitations. Corrected baseline persistent-agent fixtures pass39supervisor tests, and the first11Linux modules, including691orchestrator tests, pass. Remaining backend/image/pin/CI and owner-dependent authenticated mobile/watch/audio acceptance remain open.


## September 25 local candidate preparation

Projection source and maintained lint were reviewed and committed locally as `b5d6612b962b5d30d5fa070c69731b9f219277b6`; Windows source remains unchanged. Deep's exact component pin/gitlink and canonical UI protocol digest `5366100b215e413c85e034659a078d34c56b1f5ab44e7779f3d341eaec8d3915` now match the initialized source, and both composition verifiers pass. Current-source shared contract/assets checks pass298/298. Strict Swift lint passes with the documented Xcode27 configuration translation; Deep maintained inventory Ruff passes1461files, Projection owning Ruff passes. Gitleaks8.30.1 finds no leaks in both full histories or the staged Projection changes. Wiki screenshot/coverage checkpoint is pushed as `a88f14f`.

The pre-commit Linux diagnostic uses the previous runtime image. Its installed Projection contract resources are older than the mounted current source, so it cannot qualify the composed candidate; the clean image and canonical runner must supersede it. No current-candidate CI or release claim is made from that diagnostic.


The Apple CI review found the required watch job omitted the navigation scheme. A follow-up requires both model and navigation suites, rejects either suite failure before coverage export, and retains both raw result bundles. Apple's native `xcresulttool merge` successfully combines the local104pass/one-skip unit and10pass UI artifacts. Updated workflow/provenance tests pass89/89, including shell execution for success, either test-lane failure and merge failure; failure paths never export a success report. Native product source bytes are unchanged by this CI correction.
