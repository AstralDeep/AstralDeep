# Research — Windows UI v2

- Decision: base on Deep 4e2eae70 and Projection 1d4a864. Rationale: published 090 handoff owns console/v2 labels, catalog and geometry. Alternative rejected: Windows catalog/protocol fork.
- Decision: enable Windows in shared qualifying sets alongside tested decoder/renderers, as delegated by handoff; retain legacy fallback. No edits to 090 artifacts or Apple/Android implementation.
- Decision: preserve authenticated events, server surfaces, upload/export/share and voice machinery to preserve authority/continuity.
- Decision: use shared Open Sans/palette, native widgets and painter charts. No web wrapper or client-owned product definitions.
- Observed: current shell uses split canvas/right rail, ignores rote_config and prefers Inter/Segoe. Default interpreter lacks PySide6; install declared dependencies in ignored Python 3.11 environment.
- Vault 92e4ed0 supplies 54 references, superseding partial 470a390. Live successful result/full-screen still requires direct observation.
