# Windows UI v2 verification — 2026-09-25

Status: implemented and under qualification; not merged, deployed, released, or ready for acceptance. The owner has now authorized committing and pushing task work to its branches. Deep and Projection remain on `codex/091-windows-ui-v2`; publication status is recorded at each checkpoint. Feature 090 remains owned by the other machine.

## Source and ownership

- The initial authorized cleanup and main refresh covered AstralDeep, AstralProjection, AstralPlane, AstralPrimitives, LETS, embedded components, and kos-wiki. Ignored runtime data and credentials were preserved.
- The remote-tree preflight reserved 090 and allocated 091 once. Local spec/plan commits: `c2c80b95`, `4df082d4`, `a1783efd`.
- Exact shared handoff integrated: Deep `4e2eae707e108ee868212cec653ae107fdb15518`, Projection `1d4a8642a888279dd42a67181412901960b50217`. A fresh fetch still reports those 090 tips. Later Mac voice/Apple corrections reported in the vault are not yet published and are not dependencies of this checkout.
- Shared console/v2 labels, catalogs, actions and ROTE geometry remain authoritative. Windows negotiates the existing contract and implements six already-defined primitives. No new primitive, protocol, schema migration, runtime dependency or web wrapper was introduced.
- Windows-only disposition metadata records implemented guidance and export/share behavior. Other clients' disposition cells and 090 feature artifacts were not edited.

## Publication boundary — September 26

The owner requests immediate PR publication and cleanup, and explicitly rejects the current visual fidelity. **Screenshot parity is not achieved.** Further viewport work was stopped; its incomplete protocol/module prototype is preserved only in ignored recovery files and removed from active source. Projection is clean at `e0419dd`. The newly available shared contract is integrated, but its Windows consumer, final composed qualification and visual acceptance remain open. Earlier source-specific passes below must not be read as a completed Windows client. PRs are drafts with these blockers stated prominently.

## Commands and results

### September 26 published shared integration checkpoint

Published shared Deep `711d428210e20150e665b81931feb0512156b89e` / Projection `162486570a101cbc00f2ae48dbc33ce73c5d3308` became available during final Windows qualification. They supply the previously requested scoped viewport hydration plus shared fixture/provenance/voice repairs. Windows integrated them as Deep `f498e9e770255cc4c93c94b0197f589f0739536b` / Projection `e0419ddaad927a64c575eac21bfcc26f54378a3a`. The combined manifest digest is `25b964ce64553db80cf2789991eb3565bb86c66cc9792849aa496101ef35585d`; four-source composition and validation pass. Plane, Primitives and LETS pins are unchanged. The shared orchestrator is byte-identical to the published revision; Windows negotiation remains enabled. No Windows-authored edit touches 090 artifacts, Apple or Android. A separately owned Windows viewport consumer is now under implementation; this merge alone is not resize acceptance.

Before that integration, the `8a39237` non-stress run completed **1699 passed, 2 failed, 10 skipped, 50 warnings in546.58s**. Both failing fixtures supplied stale socket/generation setup under the new authority guards. Commit `ea121b83ae2618c34c8c1854c72720933709bd28` corrects the setup and its one evidence-ledger digest; all original ordering/privacy assertions remain. The targeted auth/voice/work-surface run passes **157 tests in8.89s**. An optional fixture-cleanup experiment caused a focus precondition failure, was reverted completely, and its failed logs remain. A subsequent broad rerun was explicitly stopped at21% when the shared dependency arrived; `windows-final-qualified-status.json` labels it superseded/incomplete, never passing. The initial broad attempt also exhausted disk while writing reports; its incomplete artifacts remain. No test deadline or assertion was weakened.

The preceding clean Deep `d105ea83` / Projection `8a39237` Linux image is `sha256:906ba6a782f69aeb6993490c01599234e43d8a1cc828de737f653a21ce53be14`. Composition, all four installed wheel digests, dependency checks, production negative exit78, configured boot/readiness and protected401 checks pass. **152 affected backend tests pass in46.13s**, with **8/8 changed lines** covered. The successful source-bound evidence is `build/windows-091/linux-final-d105ea836da1-ZO3hWk/`; two earlier setup failures are retained separately. Full-history scans pass for1144Deep/184Projection commits. These results do not qualify the newly integrated shared source.

The `8a39237` freeze passed in145.516s with unchanged inputs:101260297bytes, SHA256`32302ec1b8058d8a4cad85805dbbc7676b7656b7bf57b338b43700d8e2020a9d`. Six read-only packaged tests pass in11.28s; closure has132native entries, zero unexpected/ambient DLLs and20/20ICU symbols. Its own offline GUI/connected checks remain unrun. The earlier503b962 GUI pass remains bound to that earlier executable.

After the user completed ordinary PKCE, a current read-only native capture shows the restored six-dice total24 on `8a39237`, using the local d105image after reconnection. The inspected JPEG is1470×875 including frame; recorded client size is1470×852 atDPR1/96DPI. It is not a1440×900 logical acceptance image. The earlier file named `restored-result.jpg` was found to contain a stale366×771 image and is excluded; `restored-result-current.jpg` is the source-bound inspected capture. Desktop input/activation recovery failed with `foreground window did not report a process id`; no further input was attempted. Current attachment/Advanced/renewal interactions, native microphone/worker playback, physical scaling, touch and the complete logical matrix remain unverified.

Draft PRs will target `codex/090-native-ui-v2`. Deep's ordinary PR CI runs for that base, but Projection's core Python/web/Windows workflows filter PRs to`main`; opening these integration drafts will not automatically qualify those lanes. Local source-bound gates and final merge-target qualification remain required. No release/bootstrap workflow or publication authority is changed.

### Final session-renewal and narrow-window candidate

Projection `8a392370ce6d871df7caaad61a84ae52def22915` adds expiry-aware normal Keycloak renewal and reuses ordinary registration/resume on the same owner-bound transport. It preserves queued submissions and unchanged conversation controls while fencing retired socket, voice, host and GUI callbacks. Account replacement clears audit data and rejects late results using owner/dialog/request identity. No credentials are logged, copied between sessions or persisted by this change; no new wire contract or dependency is introduced.

Result headings now stack above their existing actions when their measured width requires it. Actionable status text wraps literal long words with native focus rendering and keyboard-scrollable overflow bounded to one-third of the window. Focused header/banner checks pass **13 tests in10.70s** with **144/145 changed lines (99.31%)**; the initial geometry failure remains recorded and its assertion was retained.

The final authentication/transport/voice/workspace/attachment cohort passes **470 tests in37.12s**. The last repair's changed executable coverage is auth **38/38 (100%)**, protocol **152/161 (94.41%)**, app **199/209 (95.22%)**. Exact commands, source hashes and the earlier failed fixture runs are retained in `build/windows-091/auth-refresh-final-evidence.json` and adjacent logs. Independent review and root inspection cover credential expiry, queued owner transitions, fresh accepted hydration and audit isolation; Ruff and diff checks pass.

The broader non-stress Windows partition is running on this immutable source, with reports `windows-final-8a39237.*`. It excludes only `test_byo_supervision_060.py`; that file, its supervisor implementation, resource accessor and fixture inputs are unchanged from the completed `3a76954` full run below. All700 process trials passed there. This split does not claim a new complete-suite run. A fresh frozen package and Linux candidate gates follow the clean composition pin. Live renewal, upload/Advanced and final UI captures remain to be verified.

The owner now explicitly requests branch pushes and PRs. Draft Windows integration PRs will target the published `codex/090-native-ui-v2` branches so the Windows delta is reviewable without taking over the other machine's feature. Shared result resize and unpublished shared CI repairs remain blockers to acceptance, not blockers to this authorized draft handoff. No merge, release or production deployment is authorized.

### Completed lifecycle baseline and new live findings

The source-frozen Projection `3a76954c374cd9c3daf7d6b1818fd2459699e1e2` run completed **1586 passed, 10 skipped, 50 warnings in1860.53s**. This includes all700 process-supervision trials. Exact command and reports are `build/windows-091/windows-full-3a76954.{log,xml,json}`, its JUnit XML and coverage data. `diff-cover` against shared `1d4a864`, with staged/unstaged edits excluded, reports **2384/2423 changed lines (98.39%)**; the90% threshold is unchanged.

Its clean frozen build completed in160.843s with unchanged inputs:101245901bytes, SHA256`27530bf07e8f28e4c0e3011765f68ae72c67ca245156c4fb8e82ac5ca4fc1f84`. Six read-only packaged checks passed in11.03s; native closure has132entries, no unexpected or ambient DLLs, and all20 required ICU symbols. This executable has not received its own GUI/connected qualification.

Later live checks exposed three remaining defects: a narrow result title and long status message can clip; a healthy socket can retain expired credentials while attachment upload returns401 and Advanced fails; committed result layouts do not readapt after resizing until History hydration. Header/banner and credential-renewal repairs are in progress and are **not qualified by the preceding frozen run**. The resize defect is coordinated through vault `d33da8ddb269cc08b82518b5ed36f27ec3955db4`: retain the completed-snapshot stale-frame guard and use the existing snapshot contract with a fresh scoped hydration request, coordinated with the shared owner. No load-chat workaround, local ROTE fork or competing protocol has been introduced.

The live narrow captures are392×875 and322×771 including the native frame; they do not establish every requested logical viewport or physical DPI setting. New Chat followed by History preserved the six-dice total24; successful export and actionable no-microphone feedback remain verified. Successful upload, post-renewal Advanced/selection, native capture/playback, physical DPI changes, tall viewports and connected packaged tests remain open. The shared090 remote anchors are still Deep4e2eae70/Projection1d4a864; later Mac repairs recorded in the vault remain unpublished.

### Resumed native verification and lifecycle fixes

Projection `3a76954c374cd9c3daf7d6b1818fd2459699e1e2` supersedes503b962 for Windows source. It rejects stale authentication callbacks after account replacement, sign-out or close; Run uses the normal send path including staged attachments while retaining the scenario prompt; attachment controls wrap, scroll into keyboard focus and expose complete filenames; category height includes polished controls even before a refreshed catalog becomes visible. Explicit unavailable voice activation shows actionable feedback without the passive web footer. No shared protocol, backend runtime, schema, dependency, Apple/Android source or090 artifact changed in this correction.

The six-file regression run (`test_auth_lifecycle.py`, `test_message_routing.py`, `test_launch_timing.py`, `test_console_shell.py`, `test_composer_geometry.py`, `test_attachments.py`) finished **170 passed, 1 failed in370.80s**. Its failing catalog-rebuild assertion exposed hidden newly added buttons being omitted from Qt layout sizing. The correction retains the visibility assertion and expands it across eight ROTE fixtures: **16 category checks passed in18.35s**, with all five new measurement lines covered on a separate16-pass coverage run. Ruff and staged diff checks pass. The full Windows suite is now running against committed3a76954; prior1537-pass results below do not qualify these new edits.

After the user unlocked Windows and completed normal PKCE, Computer Use capture/input recovered. Live working-source evidence binds `build/windows-091/live-native-fixed-source.json`; it includes the committed authentication/composer fixes but predates the final catalog-rebuild measurement correction. A real six-dice request returned **4,6,3,3,2,6 (total24)**. Preview, collapse/expand, full-screen and Escape retained values; the visible focus ring returned to the full-screen opener. The harness's accessibility focus field lagged the visible state and is not used as proof of focus. Load prompt did not dispatch. Run retained the prompt like web.

Authenticated export displayed **Canvas saved** and wrote4466bytes of server-authorized HTML containing all six rolls and total24, SHA256`c615fc4eb4089ccf56a571ecba38484df0ec5b150f9326726ff04e446e289097`. No credential/token was extracted. Clicking native Voice displayed **No microphone is available; typed chat still works.** The device probe reports microphone absent, output available, permission authorized; worker Whisper/Kokoro preflight passes in8.172s. Real capture/reply/playback/recovery remain unverified. Captures are1442×875/876 including native frame, not a claim of1440×900 logical acceptance. Saved evidence is under `build/windows-091/live-retry/` pending curated archival.

Fresh origin inspection still reports shared090 Deep4e2eae70/Projection1d4a864. The latest vault99cdcae records Mac-local Deepc18fd38d/Projection5cd0b6c and a full-CI benchmark failure; those revisions are unavailable here. The vault's owner-reported Windows-finished statement is not acceptance evidence; remaining gates below stay open.

### Final Windows qualification and branch handoff

This checkpoint supersedes the running/failing-packaging status in the dated sections below. Product source is Deep `94353536f00b1e2d18102474cf0b01cc8822a0a6` with Projection `503b96287b4fbe7ae3a87bea6cb4c4b976b2c981`; subsequent handoff edits are documentation only. The owner authorizes branch publication. Acceptance remains incomplete.

| Current gate | Exact result |
| --- | --- |
| Complete Windows offscreen suite | **1537 passed, 10 skipped, 50 warnings in 1835.28s**. Includes all 700 process-supervision trials. Python3.11.15, `QT_QPA_PLATFORM=offscreen`, `PYTHONUTF8=1`; pytest with `--cov=astral_client --cov-branch`, XML/JSON/JUnit and log `build/windows-091/windows-full-503b962.*`. |
| Windows changed-line coverage | `diff-cover` against published Projection `1d4a864`, including `windows-client/astral_client/*.py`: **2252/2298 (98.00%)**, unchanged90% threshold. `windows-full-503b962-diff-cover.json`. New freeze helper and input initialization both100%. |
| Exact frozen executable | Clean locked PyInstaller build, **194.328s**, unchanged recorded inputs; **101242703bytes**, SHA256`6cec49b1c2c4bd9691a6cec46df1b6aabe924de775705c7ebc836f64e3825bc3`. Static closure:132native entries, zero ambient-runtime DLLs; Windows ICU supplies all20required QtCore symbols. `freeze-final-503b962.json` and `freeze-review-503b962.json`. |
| Actual packaged runtime | Six read-only packaged tests **passed in18.43s**; real frozen GUI offline/retry test **passed in14.21s**; unsigned-helper integrity refusal **passed in0.41s**. Native settings were backed up in process memory, restored and compared exactly; no diagnostic window remains. `frozen-503b962-{readonly,offline,integrity}.log`, offline JSON. The previous QtCore startup failure is resolved for this executable. |
| Exact Linux candidate | Image`sha256:4e531d5a3b0a49261093bbe4628e532e04216111d47b1997444670849899a168`, labeled Deep94353536; all four component pins/digests and dependency checks pass. Missing production configuration exits78; configured production/development health/readiness and unauthenticated protected-route401 pass. |
| Affected backend on Linux | All seven snapshot/consolidation/chrome/Windows-admission modules: **152 passed, no skips, 58.81s**. Repair **8/8 changed lines and8/8 function branch arcs** covered. Exact commands/reports: `build/windows-091/linux-94353536/README.md`; evidence archive SHA256`76d5555a066881b0f8e9cfe170772c4f55978e28f9520d3dea7dce00c136c4b2`. |
| Current Projection Python | **3239 passed, 5 failed, 0 skipped in48.05s**. Failure IDs and assertions exactly match earlier ae8fdde: three Apple capability declarations, unledgered Apple fixture, shared export manifest's stale shell-source digest. Relevant files/profile cells are unchanged from shared090. Ruff,99.39%changed coverage, wheel build/install/import/dependency checks pass. `projection-ci-503b962/README.md`, archive SHA256`f357b0f5d62101bf9090b6540e12b9434b483670ae603e3bd237e0614e33fb03`. |
| Security and repository checks | Full-history Gitleaks8.30.1:929Deep/153Projection commits, no findings. Root Ruff passes; documentation links pass28files. No091delta touches `specs/090-native-ui-v2`, Apple source or Android source. |

Nine Windows skips require a candidate executable; separate packaged/integrity runs above execute seven of those cases. The two connected frozen tests still require normal valid staging authentication. The tenth skip is an inherited backend-header comparison whose standalone component-relative backend path is absent. Skips are not passes; authentication was not bypassed.

The initial-candidate canonical backend run reached its unchanged240-minute limit and was stopped gracefully (exit143, not OOM) at `2026-09-25T21:27:57.814976880Z`. Seventeen completed suites recorded **4153 passed,7 failed,7 skipped,1 xfail**. The11901-case root suite was incomplete at83%, with28explicit failed nodes and no final JUnit/aggregate; four later planned suites never started. Exact bound receipt, inventories and retained logs are in `backend-failure-diagnosis/canonical-bound-summary.json`. A clean Linux pre-repair replay reproduces the duplicate-identity failure; the current repair passes. Other shared fixture/provenance failures remain open pending published090 fixes. The separate Mac Plane4087-pass checkpoint does not rewrite this machine's retained two-timeout Plane run.

The local loopback development backend now runs the exact4e531d5a candidate image. Existing environment values and mounted paths compare unchanged; `/readyz` passes. The real existing worker image933ab6af passes configured Whisper/Kokoro speech preflight in9.703s. These observations do not establish native microphone capture or playback. The old expired native session was closed for the packaged test; fresh user Keycloak sign-in is still required for final live verification.

Both repositories trigger ordinary CI only for PRs/main pushes; publishing these work-in-progress branches alone does not establish hosted CI. No PR, merge, remote deployment, signing, tag or release is part of this handoff. See [handoff](handoff.md) for shared-fix coordination and remaining acceptance.

### DLL isolation, input capabilities, and identity-cache repair

Projection `503b96287b4fbe7ae3a87bea6cb4c4b976b2c981` isolates PyInstaller's native DLL search to the selected Python runtime and Windows system directories. The prior frozen executable bundled 48 unrelated DLLs from an ambient Codex runtime path; its ICU exports did not satisfy Qt6Core. The new build helper validates the selected directories and removes inherited Python import overrides. **66 packaging tests passed in 2.86s**; the helper has **23/23 statements and 10/10 branches covered**. The actual PyInstaller PE resolver regression verifies that an ambient competing DLL cannot win. A new freeze and actual offline GUI launch remain required; the earlier failed executable and reports are retained under `build/windows-091/frozen-dll-inspection/` and `AstralDeep-c4997f6-contaminated.exe`.

The same Projection commit initializes Qt's primary keyboard and pointing device on Windows before aggregating registered input capabilities. Qt6.11.1 initially exposed an empty device registry despite usable native input. Other platforms continue to report observed devices only. **344 capability/protocol tests passed, 1 existing backend-path skip, in 14.95s** with UTF-8 enabled; the new protocol code has **12/12 lines and 6/6 branches covered**. A native probe confirms keyboard/fine-pointer availability and no touch device on this host. These are existing capability fields, not a new shared protocol.

The Deep snapshot adapter now preserves the original component inventory in ROTE's public cache and shared chrome authorization, while separately adapting the consolidated presentation. Duplicate identities remain actionless and private-version-free after a resize when workspace lookup is absent, empty, or unavailable. Normal durable publication already rejects duplicate top-level identities; this is defense for malformed/legacy in-memory input, not a demonstrated backend authorization bypass. **147 affected tests passed with no skips in 61.78s**; a separate component-chrome branch run passed **50 tests in 7.20s**, covering **8/8 changed lines, 16/16 function lines and 8/8 branch arcs**. Ruff and diff checks pass. Exact commands, source/patch/report hashes and retained intermediate failures are in `build/windows-091/backend-failure-diagnosis/chrome-repair-evidence.json` and its README.

The complete Windows suite is running against the frozen Projection `503b962` source, including all process-supervision trials. No full-suite pass is asserted yet. The earlier complete Linux backend run remains bound to initial Deep `bbe387c9`; its unchanged 240-minute job deadline is `2026-09-25T21:27:41.776622Z`. A bound termination will be recorded as incomplete/failed, never as a pass. Product branch pushes are authorized, but merge, deployment and release remain outside this checkpoint.

### Completed CI and frozen-runtime checkpoint

The clean final source checkpoint is Deep `ff2b9293518147524ea3d4d2152d7329beba4d2f` / Projection `c4997f614ab5f8c090450761adda06bba1eac6fb`. A fresh no-cache build passes with image `sha256:7922684350bad367387ae53668159e53b6607d45841151754648f27930e1ecca`, bound to that Deep revision. Production missing-configuration exit78, configured production/development health/readiness, protected-route401 and four installed component digests pass. Release tooling passes **1663 tests, 3 skips, 4 unchanged workflow deselections in95.01s**, with **15024/16031 lines (93.7184%)**; the90% gate is unchanged. Documentation links pass28files. Full-history Gitleaks8.30.1 finds no leaks in927Deep and152Projection commits. Exact commands, source hashes, outputs and36artifact hashes are under `build/windows-091/final-linux-own/`. Boot evidence does not establish real authentication or LETS enforcement.

The full web workflow passes at that same source checkpoint: **49 Node tooling tests, 40 offline-worker tests, 1 real Chromium coverage-conversion test, 334 ordinary Chromium tests and105 responsive/keyboard/enlarged-text tests across Chromium, Firefox and WebKit**. Coverage union and strict `projection-web` changed coverage pass **163/164 (99.39%)**, required90%. Exact pinned browser image, commands, logs and coverage are in `build/windows-091/web-ci-c4997f6/run.json` and `invocation.sh`; raw evidence archive SHA256 is `b2f8cf648ce1c27b0aa46dbfcea883240bc08370c5563e7472ef2260773f1ad4`. The Python coverage input is the earlier measured `ae8fdde` XML with verified identical backend/src/scripts bytes; this does not turn the earlier failed full Python suite into a pass.

The unchanged Plane `e8b5ee62` owner gate completes **4085 passed, 2 failed, 9 Windows-only skips in3178.76s**, with **91.2315% combined coverage**, above88.75%. Lock, frozen install, Ruff and clean-source checks pass. Both failures in `test_session_restore_postgres.py` hit the existing1000ms schema-verification statement timeout; the exact two cases pass in18.86s on a new disposable database. No timeout or assertion changed. The full gate remains failed. Evidence under `plane-ci-e8b5ee62/` and `plane-focused-e8b5ee62/` explicitly records initial Deep`bbe387c9` binding and diagnostic local CPU/memory caps; this is not canonical provider acceptance.

The canonical complete backend run on initial Deep`bbe387c9` is still active. Completed persistent-agent and qual-audit suites failed; twelve implicated source/test files are byte-identical across shared`4e2eae70`, initial`bbe387c9` and final`ff2b9293`. The failures involve stale fixture contracts/patch seams and a parallel timing assertion; exact failures and baseline blobs are retained under `backend-failure-diagnosis/`. These failures remain open pending the shared090 repair/publication and complete run results.

The source-frozen PyInstaller build completes in180.468s with unchanged inputs, producing116069173bytes, SHA256`a230940dc6d4d8a1571cf0870c655d376be634d8a78fdc1aa07c9036b755ced2`. All **6 read-only packaged tests pass in14.90s**. The actual frozen offline GUI smoke fails before the window starts: `ImportError: DLL load failed while importing QtCore: The specified procedure could not be found.` Its hidden exception dialog caused the original pytest parent to exceed the90s outer safety limit; the equivalent source profile passes the unchanged offline-retry checks. Separate frozen diagnosis reproduces the DLL error. Diagnostic descendants were terminated and original HKCU settings restored and compared from an in-memory backup. Packaging is not qualified while this failure remains. Reports: `freeze-final-c4997f6.json`, `frozen-c4997f6-readonly.log`, `frozen-c4997f6-offline.json`, and `offline-{source,frozen}-diagnostic.*`.

Desktop control recovered after resetting the stale automation connection. The latest source client requires fresh user Keycloak sign-in; no authentication was automated. Current native Windows screen observations are2560×1440 and1920×1080, both DPR1.0/logical96DPI. After initializing the primary pointing device, Qt reports a position-capable mouse; Windows reports digitizer flags0 and maximum touches0, so touch is unavailable in this session. Physical scaling transitions, authenticated final-source resizing/attachments/export/selection and native microphone/worker/playback remain open. The owner sign-in request is pending.

Latest source checkpoint: local Projection `c4997f614ab5f8c090450761adda06bba1eac6fb` follows the initial `ae8fdde` / Deep `bbe387c9` candidate. Final review found and repaired account-switch draft/attachment retention, export/share availability before matching conversation hydration, a connection-rotation delivery window, and full-screen focus reaching deferred-deletion controls. Same-owner refresh preserves drafts; owner changes (including unreadable identity) erase them and reject late upload completion. Workspace actions require the exact rendered snapshot, chat and current connection. Seven authority regressions, seven settled-event keyboard regressions and 47 attachment/continuity/auth checks pass.

The final frozen-source non-stress Windows partition passed **1493 tests, 10 skipped, 50 warnings in 142.12s**, with current coverage in `windows-final.*`. The previous failed partition is preserved as `windows-before-owner-review.*`. Unchanged supervision trials passed in the earlier full run; the latest partition excludes that file explicitly and is not presented as a new full-suite pass. No test or timeout was weakened. Thirty-two inspected synthetic shell captures cover eight states at simulated Qt scale factors1/1.25/1.5/2; their exact source hashes and patch digest are retained separately from physical display acceptance.

Clean Linux qualification of Deep `bbe387c9` / Projection `ae8fdde` built image `sha256:9d377d857f197e423d4867ff02d32bd26081ee0cace52a2e840d8c377c535dcd`. Production missing-secret boot exited78; configured production and development health/readiness passed against disposable PostgreSQL17. These are boot checks, not real-authentication or LETS enforcement qualification. The canonical complete backend suite is still running with separate per-suite databases. The isolated voice-worker runtime/test build and coverage gate passed **372 tests, 2 skipped in 7.88s**, **4094/4537 lines (90.24%)**; this does not establish native microphone/media success.

The clean Linux Projection run passed **3239 tests and failed5 in52.66s**, with no skips: three inherited Apple disposition cells, an unledgered Apple fixture, and a stale export source digest. Its immutable ledger audit also found16 stale Windows-owned entries introduced/affected by091; these were repaired from canonical staged Git blobs in `c4997f6`, preserving all unowned/global provenance fields. The remaining24 stale/missing inherited entries and unledgered Apple fixture remain a shared090 publication dependency. ROTE changed coverage passes100% versus090. Exact reports are under `build/windows-091/projection-ci-ae8fdde/`.

The original Projection CI packaging recipe failed on both candidate and immutable shared `1d4a864`: pip's hash-bearing constraint activates hash enforcement while the build frontend creates an unhashed isolated setuptools requirement. The minimal correction appends `--no-isolation` after the already hash-locked toolchain install; dependency locks remain unchanged and a workflow regression enforces the ordering. **64 workflow tests**, corrected sdist/wheel build, installed imports and dependency checks pass. The SDK gate passed **46 tests, 1 optional-dependency skip**; component contracts passed **273 tests, 3 existing CI deselections**; source-free composition declarations passed. Logs and exact identities are in `build/windows-091/owner-gates-bbe387c/`. Complete-history secret scans found no leaks in926Deep commits and151Projection commits at the initial candidate.

Commands run from the Deep root unless the component directory is stated. Host Python is CPython 3.11.15. Host Qt tests set `QT_QPA_PLATFORM=offscreen` and `PYTHONUTF8=1`. Logs and coverage reports are diagnostic, ignored artifacts under `build/`; they contain no acceptance waiver.

| Gate | Exact invocation / evidence | Result |
| --- | --- | --- |
| Full Windows suite | `.venv/Scripts/python.exe -m pytest components/AstralProjection/windows-client/tests -q --tb=short --cov=astral_client --cov-report=xml:build/windows-091/windows-complete.xml --cov-report=json:build/windows-091/windows-complete.json`; `COVERAGE_FILE=build/windows-091/coverage-complete` | **1470 passed, 10 skipped, 50 warnings**, 1688.65s. Includes all 700 process-supervision trials. Log `build/windows-091/windows-complete.log`. This run precedes the final resize-envelope correction; the affected transport suite and full non-stress partition are rerun after that correction. |
| Failed full run retained | Same scope, reports `windows-full-stable.*` | **1 failed, 1462 passed, 10 skipped**, 417.22s: `test_hundred_trials_per_behavior_leave_no_worker_resources` exceeded its existing cleanup deadline, reporting 6.765s. No deadline or assertion was changed. The later complete run above passed. |
| Earlier interrupted full run | `build/windows-091-progress.log` | All 16 supervision tests passed; later a native Qt access violation occurred during a composite paint test. A complete later run passed without relaxing paint tests. Do not count this interrupted run as passing. |
| Voice failure cleanup | `python -m pytest components/AstralProjection/windows-client/tests/test_voice_lifecycle_065.py components/AstralProjection/windows-client/tests/test_voice_refusal_reasons_066.py -q --tb=short` | **50 passed**, 2.05s. Checks authenticated server-session cleanup, cleanup refusal, closed/rotated authority, missing binding, and actionable error text. |
| Theme transition | `python -m pytest components/AstralProjection/windows-client/tests/test_console_shell.py components/AstralProjection/windows-client/tests/test_theme_live.py -q --tb=short` | **56 passed**, 12.88s. A test originally restored the palette using the wrong input shape and leaked Daylight into a later test; corrected restoration, unchanged assertions. |
| Deep chrome | `python -m pytest backend/tests/chrome -q --tb=short` with the existing backend test setup | **667 passed, 25 skipped**, 41.24s. The database slice is verified separately below. `build/windows-091-deep-chrome.log`. |
| PostgreSQL native guidance | `python -m pytest backend/tests/test_native_guidance_postgres.py -q` with `ASTRALPLANE_TEST_POSTGRES_DSN` pointing at disposable PostgreSQL 17 | **18 passed**, 299.71s host; **18 passed**, 357.52s Linux reproduction. Logs `windows-091-guidance-postgres*.log`. No production database was changed. |
| Deep UI v2 CI slice | The 11 explicit UI-v2 contract/surface files from the current CI workflow | **340 passed**, 10.62s. Result log: `build/windows-091-ui-v2-gates.log`. |
| ROTE and console model | `python -m pytest components/AstralProjection/tests/rote components/AstralProjection/tests/chrome/test_console_model.py -q --tb=short --cov=rote --cov=webrender.chrome.console_model --cov-report=xml:build/windows-091-rote.xml`; `PYTHONPATH=components/AstralProjection/backend;components/AstralProjection/src` | **1174 passed**, 4.42s with UTF-8 enabled. `build/windows-091-rote.log` and XML. |
| Projection full host suite | `python -m pytest tests -q` from Projection | **3151 passed, 86 failed, 1 skipped** before the Windows disposition repair. Replayed the 86 failures on an isolated clean `1d4a864` worktree: **85 failed, 1 passed**. The candidate-only Windows inventory issue was fixed; the others are inherited/shared/platform failures. |
| Projection full Linux suite | Locked tooling in a Linux container, mounted Projection `tests` | **3228 passed, 9 failed, 1 skipped**, 124.96s. Windows metadata was subsequently fixed. Remaining failures cover three inherited Apple disposition cells, transformation/export evidence digests, and host-mount workflow/executable/symlink assumptions. `build/windows-091-projection-linux.log`. This gate is not green. |
| Disposition matrix | Projection `tests/test_disposition_matrix_088.py` | After Windows-only repair: **78 passed, 3 failed** (unchanged iOS/macOS/watchOS cells). Strict refusal mutation tests retained. |
| Web tooling | From Projection `tooling/web-ci`: pinned `corepack npm ci --ignore-scripts`, package-manager/product-isolation checks, isolation tests, ESLint | Install passed (80 packages, zero reported vulnerabilities); pinned npm 11.16.0, isolation checks and lint passed. No web product source changed in 091. |
| Web browser CI slice | Current explicit 334-test Playwright selection with Chromium headless 1228 / Playwright 1.61.1 | **317 passed, 17 failed** because native-export subprocesses expected `PYTHON_BIN`; rerun that whole file with the correct root Python: **17 passed**, 24.4s. All 334 passed across these runs. Logs `windows-091-web-tests-ready.log` and `windows-091-web-native-export.log`. |
| Python lint | Root touched backend files plus Projection Windows, ROTE and disposition files | Ruff passed; `git diff --check` passed (CRLF normalization warnings only). Repeat on the final tree. |
| C# helper | .NET 10.0.400, locked restore, both `dotnet format --verify-no-changes --no-restore`, Release build, deterministic tests and host-capability test | Zero build warnings/errors; **37 deterministic tests + 1 real installed-SAPI capability test passed**. Two clean publishes have identical executable SHA256 `975d5a4640b9cd9a0ded071a738a22f0981112b543c572144a92d615a031dac6`. `build/windows-091-packaging.md` records exact commands and targeting-pack setup. |
| Helper provenance | Existing voice-package suite after repairing three stale source digests | **7 passed**. The three old digests matched pre-comment-cleanup source; C# product code was unchanged. |
| Freeze | New release-only venv installed from `windows-client/requirements-release.lock.txt` with `--require-hashes`, then `python -m PyInstaller --noconfirm --clean AstralDeep.spec` | Passed in 208.955s; executable 116061992 bytes, SHA256 `294f66e8a045c61facd22dc5eca65f2e392fdfaa35ee5a1f0483da3220f961b8`. Predates the final resize-envelope fix and must be superseded. |
| Frozen read-only smoke | Candidate EXE via `ASTRAL_WINDOWS_EXE`; packaged-release tests excluding GUI/registry-sensitive cases | Earlier helper-integrity plus packaged read-only selection: **25 passed**, 17.98s. Latest freeze read-only packaged selection: **6 passed, 3 deselected**, 13.56s (`build/windows-091/final-frozen-readonly.log`). Connected and offline GUI qualification remain separate. |

Latest review regressions: `python -m pytest components/AstralProjection/windows-client/tests/test_console_capabilities.py components/AstralProjection/windows-client/tests/test_console_shell.py components/AstralProjection/windows-client/tests/test_voice_lifecycle_065.py -q --tb=short` passed **152 tests in 14.25s**. This includes keeping a pending settings apply timer/status through theme restyling, checking voice session authority after acquiring the cleanup lock, refreshing viewport negotiation after registration, and suppressing errors from superseded socket writes. The actual Windows `update_device` producer is exercised against server frame admission by `python -m pytest backend/tests/chrome/test_windows_console_admission.py -q`: **5 passed in 2.25s**.

The earlier non-stress partition (`--ignore=components/AstralProjection/windows-client/tests/test_byo_supervision_060.py`) passed **1456 tests, 10 skipped, 50 warnings in 52.90s**, before final review changes. The later full run (`windows-review-full.*`) finished **1 failed, 1474 passed, 10 skipped, 50 warnings in 1754.24s**: a startup test's `inspect.getsource` read source edited after module import and failed to locate the deferred call. The unchanged process-supervision trials passed. Source is now frozen during qualification.

The frozen-source non-stress run (`windows-final.*`) finished **1 failed, 1478 passed, 10 skipped, 50 warnings in 96.14s**. The inherited BYO atomic installation path returned `WinError 5`; the inventory test then found its initial revision missing. This failure is retained and under diagnosis; no retry, deadline or assertion was relaxed. Rerunning both complete files `test_byo_runtime_compatibility.py test_win_agent_startup_gate.py` passed **42 tests in 5.49s**. This scoped pass does not relabel either failed aggregate run as green.

Final production-style composer/shell checks passed **62 tests in 28.53s**; after correcting idle voice-button vertical alignment, the complete composer geometry file passed **11 tests in 8.95s**. The final non-stress partition includes both files. Current Deep console, Windows resize admission and watch registration harness checks passed **41 tests in 5.49s**, with native-console coverage (`deep-final.xml`). Root `ruff check .` passed and `scripts/check_doc_links.py` checked **28 Markdown files**. Checksum-pinned Gitleaks 8.30.1 scanned root history (**925 commits, 62.02 MB**) and the staged Projection changes (**284.59 KB**) with **no leaks**; full candidate history scans follow local commits.

The UI-v2 CI selection is exactly `backend/tests/test_client_js_contract.py backend/tests/test_turn_selection_surface_089.py backend/tests/test_typesafe_layout.py backend/tests/test_typesafe_budget.py backend/tests/test_typesafe_decision.py backend/tests/test_typesafe_security.py backend/tests/test_rail_parts_062.py backend/tests/test_history_write_failures.py backend/tests/chrome/test_topbar.py backend/tests/test_chrome_events.py backend/tests/test_voice_deployment_topology_065.py`.

The host component sync rebuilt/installed the current local wheels but `pip check` failed because the mixed developer environment contains `presidio-anonymizer 2.2.364` requiring `cryptography>=48.0.1`, while the Windows release lock pins 46.0.7. Release qualification uses a separate hash-locked environment; do not weaken either lock to conceal the host conflict. The initial missing packaged `contracts` namespace was resolved by rebuilding the component wheels.

## Coverage

The final executable changed-line measurement compares against exact Projection handoff `1d4a864`, includes new files, and intersects diff lines with coverage executable statements. Standard `diff-cover ../../build/windows-091/windows-final.xml --compare-branch 1d4a8642a888279dd42a67181412901960b50217 --fail-under 90 --include 'windows-client/astral_client/*.py' --format json:../../build/windows-091/windows-diff-cover.json --total-percent-float` from the clean committed Projection tree passed **2220/2266 (97.97%)**. Per-file changed coverage: app501/535 (93.64%); console widgets663/675 (98.22%); composites385/385; console147/147; protocol135/135; workspace actions250/250; voice114/114; theme19/19; renderer1/1; REST5/5. Constant-only icon/manifest changes introduce no measured executable statements. Reports are `windows-final.xml/json`, `windows-diff-cover.json` and `changed-coverage.json`. Deep's one changed executable line passes100%; the complete backend gate remains separate.

## Live observations and corrections

- The user performed web Keycloak sign-in and native PKCE sign-in. No sign-in, token manufacture, auth bypass or credential logging was performed by the agent.
- Live web `https://sandbox.ai.uky.edu/` was inspected at 1440×900 and 390×844. A real six-dice result (total 24), collapse/expand and full-screen retained values; Escape restored opener focus. Four supplementary captures are in the vault. The browser returned JPEG bytes; the archive now uses `.jpg` filenames and decoded dimensions (1440×900 / 390×844), preserving the original byte hashes.
- A source diagnostic launcher connects through normal Keycloak to the rebuilt local backend (`ws://127.0.0.1:8001/ws`) with token fallback disabled. This is not packaged-profile acceptance. The healthy candidate image and real worker run locally; `/readyz` passes.
- Windows ran a real six-dice request through ordinary dispatch (5, 1, 3, 5, 6, 3; total 23). Result preview/full-screen retained the values. Escape restored the full-screen opener. Load prompt did not dispatch. Run now replaces any earlier draft before dispatching exactly once.
- Initial legacy welcome components were incorrectly presented as a result; the known server welcome IDs are now excluded from result presentation. Agent cards were too short and were corrected. Live Daylight exposed stale inline shell/dialog colors; corrected restyling retains unsent input and control state, with regression coverage.
- Account navigation, server-owned theme settings and More → Advanced were opened live. The Advanced picker correctly reported no available revisions for this account. Midnight was restored after checking Daylight. Selection with an actual published revision remains a gap.
- Export reached the native save dialog in the initial session. After its token expired, it correctly refused with sign-in feedback. A saved-file verification is still pending. Share was not offered by this local server's catalog; no permission flag was forced. Native export consumes the authorized committed server HTML; the separate native component-state finalizer remains outside the currently published Windows contract.
- Real voice activation failed with `network_interrupted`; successful recording, worker reply and playback are **not verified**. LiveKit's advertised address matches this host's loopback setup. Local media teardown discarded identifiers before server cleanup, leaving server-owned controls stale; the client now captures the authorized session scope, tears down local media, and releases exactly that server session without late UI mutation. It also translates the failure into actionable text. The Mac vault reports separate successful voice fixes, still unpublished; do not duplicate its shared protocol/work.
- Live resize investigation found that Windows omitted the existing required submission/request UUIDs and sent a non-chat session placeholder on `update_device`. The corrected current-connection envelope uses the existing admission contract. Live confirmation and narrow composer/voice geometry remain pending.

## Remaining acceptance gates

1. Recheck live resizing after the corrected envelope at1440×900,1280×800,1024×768,834×1194,768×1024,390×844 and320×740; touch hardware availability; keyboard navigation and focus; real Windows DPI/monitor transitions. Extra1920-wide coverage is useful but does not replace the required834×1194case. Offscreen fixture dimensions alone are insufficient.
2. Verify the corrected theme transition, native voice media/cleanup/recovery, attachments, Advanced with real revision selection, conversation reopening/state, and a saved authorized export. Record unavailable share/catalog capabilities honestly.
3. Connected frozen smokes require valid staging through normal user authentication. The final503b962 executable passes the actual offline GUI/retry test and all separate archive/helper checks; native settings restoration is verified.
4. Finish latest coverage, affected admission tests, final lint/composition/CI checks and security review. Inherited shared Projection failures and complete backend CI remain unresolved, not waived. Do not modify another machine's 090 artifacts to hide those failures.
5. Keep published Windows handoff branches and the isolated shared identity fix coordinated with090. Current source/pins are committed and locally qualified to the exact limits above; full acceptance remains open. Refresh and commit/push curated vault pages, image metadata, index and log at each checkpoint. Never stage ignored runtime artifacts, credentials, generated user code, or user data.
