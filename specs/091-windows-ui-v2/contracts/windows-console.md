# Consumer contract

Consume published Projection `162486570a101cbc00f2ae48dbc33ce73c5d3308` and Deep `711d428210e20150e665b81931feb0512156b89e`. Shared chrome, ROTE, fixtures and `presentation_contracts.console_v2.viewport_refresh` remain authoritative. The initial `1d4a864` foundation is superseded by this coordinated revision.

Registration uses `console_contract: console/v2` and exact supported types. `chrome_menu.model.console` supplies content; `rote_config.device_profile.console` supplies layout. Legacy negotiation retains server fallback. Add Windows qualification with the implemented consumer.

Run uses authenticated chat dispatch; Load only edits the draft, including exact-current-surface `compose_prompt`. Advanced uses `chrome_turn_selection_set` and correlated `chrome_surface.selection`. Preserve actionable errors and owner boundaries.

Preview/fullscreen reuse one canvas. Export/share retain owner/chat/render-revision validation. Do not create the web-only passive voice banner.

Resize hydration requires literal `rote_config.viewport_snapshot_supported == true`. Coalesce observed capability changes and send current-connection-only `update_device` with fresh UUID4 submission/request IDs, active chat, exact connection generation, settled `base_render_revision` and `snapshot_purpose: hydration`. Never queue across reconnect or invoke `load_chat` automatically. Legacy servers retain ordinary capability updates.

Hold correlated layout acknowledgment until the matching canonical snapshot validates at exactly the declared semantic revision. Defer during commits, ordinary hydration, editing, voice, uploads, exports and active surfaces/work. Retain the committed view, composer, attachments, selection and presentation state while waiting or on failure. Preserve editable state by unambiguous semantic identity, never positional control order. Retire only the exact request on timeout/refusal; expose retry. Authentication loss, disconnect, owner/chat changes and competing semantic work retire pending refresh authority and timers. The completed-generation transient guard remains unchanged.
