# Tasks: Canvas-First Adaptive UI/UX

**Input**: [plan.md](plan.md) · [spec.md](spec.md)
Status legend: [ ] open · [x] done · [~] in progress

## Phase P0 — Groundwork (bug fixes; code committed, tests here)

- [x] T001 bind_chat: coordinator + protocol + InMemory + Postgres repositories (None→chat only, fence-checked) — committed `38ce6ac2`
- [x] T002 `_adopt_operation_chat` wired after chat creation in the ui_event chat_message branch — committed `38ce6ac2`
- [x] T003 `openai_auth_kwargs` + shared keyless httpx client; applied at client_factory, probe, REST probes, agent codegen, knowledge synthesis — committed `38ce6ac2`
- [x] T004 `custom` preset key-optional + surface copy + pinned providers test update — committed `38ce6ac2`
- [x] T005 Tests: bind_chat unit — None→chat adopt, idempotent re-bind, cross-chat refusal, already-scoped refusal, stale fence ([test_bind_chat_066.py](../../backend/tests/test_bind_chat_066.py); live first-message verified in browser; Postgres-repo variant exercised via the shared contract in CI)
- [x] T006 Tests: keyless auth — real-key passthrough, keyless transport, shared client, header stripped at the wire, key-optional custom ([test_keyless_auth_066.py](../../backend/llm_config/tests/test_keyless_auth_066.py))

## Phase P1 — Web core (layout, composer, envelope)

- [x] T010 shell.html: composer rework (default voice control + SVG, connection pill, turn-status line, chat toggle w/ unread badge, rail header + collapse) — verified live
- [x] T011 astral.css: three layout modes, floating composer, overlay drawer, SVG icons via client injection, queued/error styles, bounded canvas, calm chrome reveal, canvas toolbar surface — verified live at 520/800/1264px
- [x] T012 client.js: layout-mode machine + localStorage pref + toggles + unread badge/peek (reduced-motion aware) — verified live
- [x] T013 client.js: connection pill + bounded queue (5, 45s TTL) for chat AND chrome actions, flush on the rote_config verdict, refusal restores text (SC-003 drill pending in T040)
- [x] T014 client.js: default voice control before frames + on teardown; composer_state refines (verified live: enabled when worker admitted, default when absent)
- [x] T015 Capability envelope live: `capability_update` action (client debounce + server handler + rote_config re-push) — verified live (browser→tablet→browser reclassification in logs, no reconnect)
- [x] T016 rote/capabilities.py: additive `reduced_motion` + `pointer_type` fields

## Phase P2 — Lifecycle & chrome

- [x] T020 Turn failure UX: transient bubbles materialize into the rail on terminal failure + inline error card with exact-text ↻ Retry; canvas never blanked (verified live pre-fence-fix); client stuck-request watchdog deliberately dropped (R6)
- [x] T021 Turn phases DELIVERED with tool identity. Three defects fixed: (a) `chat_step` carries no generation, so the 060 fence dropped **100%** of step frames on web (making `renderStep` dead code) and on Windows — both now scope by chat id like Android/Apple; (b) `chat_status.message` was discarded for a 4-key map that also omitted `fixing/retrying/combining/condensing` (those frames CLEARED the line); (c) the decisive one — a generic `operation_status` label "Working…" fires 1s into EVERY operation and, with last-writer-wins `setStatus`, clobbered the turn's own phase (on a tool-less turn nothing re-asserted it). Agent identity is derived from the `agent_list` the web client now requests (`discover_agents`) plus `tool_progress.agent_id`, with collisions treated as ambiguous. **Verified live**: Submitting → Accepted → Analyzing request and planning actions → Planning next step → Running: get_weekly_forecast, get_daily_forecast → `get_weekly_forecast — Weather Agent` → `get_daily_forecast — Weather Agent` → Designing your layout
- [x] T022 Markdown in the rail DELIVERED. Root cause: the 062 words-only filter keeps only the raw markdown SOURCE, and the `_presentation` envelope was added to `components` parts only, so the client rendered text with `textContent`. Assistant text parts now carry the same transport-only envelope through the identical escape-first pipeline; `plain_md()` strips markup from previews at the single `get_recent_chats` choke point. **Verified live** on a reloaded transcript + preview. 16 pins.
- [ ] T023 Boilerplate → metadata: **REOPENED with a stronger verified finding (2026-08-04) — the previous "zero wire change" premise is FALSE.** The earlier verifier's fix (carry the lifted primitive's variant on the rail part) was implemented and then reverted the same day: canonical transcript text parts are EXACTLY `{"type","text"}`, enforced in three places the earlier pass missed — server-side `ConversationSnapshot._validate_part` ([shared/protocol.py:1590](../../backend/shared/protocol.py#L1590)), Apple's exact-key `ConversationPart.init` ([ConversationContinuity.swift:100](../../apple-clients/AstralCore/Sources/AstralCore/Protocol/ConversationContinuity.swift#L100), which silently DROPS a variant-carrying part), and the 060 `native == original` snapshot pin (`test_conversation_snapshot_060.py`). A real caption turn would have failed continuity-frame validation at commit. The canonical boundary is now pinned by [test_rail_caption_variant_066.py](../../backend/tests/test_rail_caption_variant_066.py) (caption lift stays `{type,text}`; the validator's refusal of a variant key is itself a test). Closing T023 for real = a deliberate cross-client contract extension: optional bounded `variant` on canonical text parts across the server validator + Windows/Android/Apple decoders + the 060 contract doc, then the one-line carries in `_rail_parts`/`augment_text`. Earlier facts still true: the original Card-branch collapse breaks the FR-027 pins (`test_ui_designer_integration.py:394/402`) and touches `_narrate_job_result`.
- [x] T024 Titles: root cause was the keyless 403 (fixed); deterministic fallback added on title-LLM failure
- [x] T025 Component chrome hidden at rest; hover/focus reveal + coarse-pointer tap toggle — verified live (dashboard at rest shows zero chrome rows)
- [x] T026 Canvas toolbar: solid backdrop surface, sticky without see-through overlap
- [x] T027 Welcome on new chat (ordered before chat_created) + wel_ provenance-stamp exclusion — verified live

## Phase P3 — Voice reliability & diagnosability

- [x] T030 GET /api/voice/status DELIVERED (2026-08-04) per the verified scope: `WorkerStatusEntry`/`AdmissionRefusal` dataclasses beside the untouched `WorkerPoolReadiness` + `WorkerPool.worker_status()` (same liveness/replacement filters as `readiness()`, so the list always explains the counters); bounded `AdmissionRefusalLog` (8 entries, newest first, reason sanitized to the content-free code alphabet) recorded at exactly the three genuine refusal exits — pre-accept auth failure, registration timeout, registration refusal — with a `receipt is None` guard so an admitted worker's later protocol violation never records, and a pin that the healthy challenge-issue leg records nothing; `VoiceServices.voice_status()` projection; route with its own rate-limit bucket (status polling can never starve `/capability`). Every refusal also emits the FR-035 structured line `voice_worker_admission_refused stage=… reason=…`. 15 pins in [test_voice_status_066.py](../../backend/orchestrator/tests/test_voice_status_066.py). **Live-verified on the Windows box** against the real compose stack: admitted worker projected; a bogus-signature upgrade recorded `{stage: authentication, reason: invalid_challenge}` while `ready` stayed true — exactly the sandbox stale-secret diagnosis view.
- [x] T031 Worker preflight re-check DELIVERED (`preflight_until_ready`, 5s→60s bounded backoff, per-attempt reason logging, fail-fast on `missing_credential`). This is the failure mode behind the sandbox 503: a worker that lost its preflight died silently and, under staging's `restart: "no"`, stayed dead. 5 new pins; 313 voice tests pass. **NOTE**: the proposal's companion `control.py` edit was REJECTED — it used `sys` without importing it and would have killed the worker on the first transient reconnect, shipping green because both `run_worker` tests stub `run_forever` wholesale.
- [x] T032 Composer refusal reasons DELIVERED (2026-08-04), both defects, inside the existing `VOICE_REASONS` vocabulary: (a) `VOICE_REASON_TEXT` now covers every server refusal reason (worker/asr/tts/voice unavailable, capacity_exhausted, feature_disabled, authentication_required, permission_not_determined, output_language_unsupported, chat_context_unavailable) so a session-create 503's `code` renders its own honest line instead of the generic error fallback — drift-pinned BOTH directions against `composer_model.VOICE_REASONS` in [test_voice_composer_reasons_066.py](../../backend/tests/test_voice_composer_reasons_066.py) (missing copy fails; invented reasons fail); (b) the 30s activation timeout branches on `pending.awaiting_permission` (set/cleared around `acquireVoiceMicrophone`) and reports `permission_not_determined` while the browser prompt is open, `network_interrupted` otherwise. **Live-verified in the browser** (mic-blocked pane → honest permission line). Parity note: Apple already renders honest lines (T051); Windows shows the honest raw CODE (`voice.py:2252` renders `str(reason)`) — human copy there recorded as a follow-up, not silently changed.
- [ ] T033 Prod remediation (operator): run voice-prod-diagnosis.md runbook on sandbox; record outcome — OPERATOR-GATED
- [x] T034 docs DELIVERED (2026-08-04): [production-deployment.md](../../docs/production-deployment.md) now records the full FR-037 topology contract — voice vhost reverse-proxy rules (Apache snippet, `/rtc` + `/twirp`, media does NOT traverse the proxy), the environment contract table incl. `VOICE_WORKER_CLOSURE_SHA256` provenance (recompute from the DEPLOYED image; zero digest is dev-only, boot-refused in production) and `VOICE_COORDINATOR_REPLICA_ID` (per-replica lease owner, boot-refused when unset in production), restart-ordering/self-heal guarantees (FR-035/FR-036), and the diagnosis runbook wired to the new `GET /api/voice/status` first stop.

## Phase P4 — Verification, native consistency, handoff

- [~] T040 Browser sweep DONE for widths + chrome + failure drill: 500px stacked/100% canvas/36ch · 764px collapsed/100%/62ch · 1034px split/69%/40ch · 1584px split/73%/54ch · 0 chrome rows at rest · failure drill shows message + inline retry + preserved canvas. REMAINING: the scripted ≥10-send adverse-connection suite (SC-003) is manual-only so far
- [~] T041 Backend root lane after ALL 066 work: **5920 passed** (17 failed / 26 errors), and the failure set is strictly SMALLER than the pre-066 baseline — topology 4→0, conformance 2→1, everything else identical, **zero new failures**. Every 066-caused failure was fixed (manifest action, client-js contract, LLM surface contract, voice-dispatch double, analysis render). Each remaining failure was traced to a repo file the image does not bake and proven to disappear once copied in: `deploy/` + `docker-compose.voice-integration.yml` cleared the topology set; `apple-clients/` cleared one conformance failure and `android-client/` accounts for the last. Still artifact-blocked (need a BUILT worker image / evidence bundle, not source): voice-worker closure + packaging, release-evidence bootstrap, prepare-release-evidence. **Ruff clean** with the CI-pinned 0.15.21 from the repo root. Diff-coverage pending CI. Voice package: 313 passed. Android: 248 passed.
- [x] T042 Windows parity: canvas leads / rail trails (QSplitter reordered, canvas stretch=1, non-collapsible) **plus** the `chat_step` guard fix (Windows was dropping every step frame for the same reason web was). Suite green after BOTH changes: **701 passed / 7 skipped** — byte-identical to the pre-change run, so no regression. Suite run blocked locally until `PySide6-Addons` was installed for QtMultimedia (recorded in quickstart)
- [x] T043 Android parity: SplitShell reordered canvas-then-rail; **248 unit tests pass**
- [x] T044 [apple-handoff.md](apple-handoff.md) + [parity-checklist.md](parity-checklist.md) + screenshots/README (web captures; Windows/Android captures flagged as not taken this pass)
- [~] T045 Non-regression: the 055 loading contract, workspace identities, exports and a11y landmarks are covered by the passing suites; voice flow re-check pending a live mic run

## Follow-ups discovered during verification (not in the original plan)

- [x] F-A `execution_lease_expired` on a slow turn emitted a terminal failure for a turn that then SUCCEEDED — failure notices are now retracted on a later completion for the same request generation. The underlying lease-vs-slow-LLM behavior (a 060 concern) is left as-is and flagged.
- [x] F-B The shared keyless httpx client was closed by the OpenAI SDK on instance finalization, breaking every keyless call after the first (pinned by test).
- [ ] F-C UK LLM factory intermittently returns 504 / takes ~29–58s for short completions under load. External; affects perceived turn latency, not correctness.
- [x] F-D Windows top bar + composer style parity (P11). Windows was the last client rendering text labels where web and Android render icons. Fixed: top-bar controls are icon-only with the name in the tooltip + accessible name (order and server model unchanged); voice controls render the server's `icon` through a glyph map mirroring web's `VOICE_ICONS`; the permanent "Voice: off" chip is hidden when off with nothing to report, matching web's `hidden = state === "off" && !message`; the paperclip joins the same 38px ghost-button treatment. One latent defect fell out: `_ACTION_ICONS` was keyed on `pulse`/`activity`/`clock`, names the server model **never sends**, so `sparkle` (Pulse digest) fell through to a text label — the map now uses the model's own names (`sparkle`/`history`/`gear`) and a test pins that every one resolves. Verified live (icon bar rendered; a `roll 2 dice` turn round-tripped to a canvas card + bold rail text). Suite: **687 passed / 7 skipped** with the change.
- [x] F-E Export page / Share page moved out of the sticky in-canvas toolbar into the top bar, revealed only while the canvas carries the matching workspace flag. The natives never had such a bar — Windows `component_menu` and Android `ArtifactChrome` both expose the same two actions from the component context menu — so the persistent web bar was the outlier, and it cost a permanent strip of the primary surface. Same classes, same delegated handlers; only the DOM location changed. Verified live in the browser: buttons present in the top bar, export shown / share hidden per flags, `#astral-canvas-toolbar` gone.
- [ ] F-F **A signed Windows release is blocked on operator setup, not on code.** The release path is `build-windows-candidate.yml` → `release-readiness-protected.yml` → `release-windows-publisher-controller.yml` (environment `release-publisher`) → `release-windows.yml`, the bridge signer whose OIDC identity `windows-client/astral_client/integrity.py` pins as `…/.github/workflows/release-windows.yml@refs/tags/<tag>`. Two prerequisites are absent: the repo has **no** `staging_access_token` secret (the candidate builder's packaged smoke steps need it) and **zero** repository variables, so `RELEASE_TRUSTED_BUILDER_SHA` is unset. `build-windows-candidate.yml` has never run, and `workflow_dispatch` on it requires `refs/heads/main` — this branch is not merged. A hand-made `gh release create` would carry no sigstore bundle and the shipped v0.3.0 updater would refuse it, besides colliding with the publisher's tag-collision check for a later proper release. Source version is 0.4.0; latest public release is v0.3.0.
- [x] F-G **Release-hygiene catch**: the tracked production `windows-client/deployment/release-profile.json` + `runtime-manifest.json` had been overwritten with a LOCAL dev profile (`astraldeep-local-devbox`, `ws://127.0.0.1:8001/ws`, `development_defaults_allowed: true`) to point the client at the local orchestrator. Reverted to the production identity (`astraldeep-production-uky-0.4.0`, profile digest `771bd01c…`) before any commit — it would have poisoned a release build. Local dev launches must pass a profile on the command line (`command_line_profile_allowed`), never edit the tracked one.

## Phase P5 — Apple pass + voice latency (Mac agent, 2026-08-03)

- [x] T050 Apple canvas-first layout: width-driven three-mode machine (stacked <700pt / collapsed 700–1023 / split ≥1024, `@AppStorage` pref, width bound beats pref) in `ChatShell`; `SplitShell` FLIPPED to canvas-leading/rail-trailing with a 320–420pt clamped rail + header collapse; NEW `CollapsedShell` (full-width canvas, `safeAreaInset` floating composer bar max 760pt, transcript drawer with unread badge + pin-rail); macOS `defaultSize` 1280×820. All 063.2 layout firewalls preserved (outer GeometryReader hands concrete sizes; drawer ChatList fixed height; CanvasArea firewall untouched). **Live-verified on macOS**: split with a real dice turn, collapsed, drawer, pin round-trip; iPhone stacked verified on the signed-in sim. iPad mapping decided = web breakpoints.
- [x] T051 Apple voice affordance (P5): disabled default mic before the first `composer_state` on iOS/macOS AND watch (web-parity); `messageFor` no longer fabricates "Voice is available." for `unavailable`/`worker_unavailable`/`feature_disabled`/`capacity_exhausted`/auth reasons (live-verified honest text against the no-worker stack); status line hides only the off/ready boilerplate (Windows quiet-set twin).
- [x] T052 Apple P11: voice chips icon-only (labels stay as a11y name + tooltip; `voice-control-<key>` ids preserved; disabled chips dimmed); `gear` added to the top-bar icon map; watch speaker-stop/muted/consent SF Symbols unified with iOS/macOS.
- [x] T053 Capability envelope: additive `reduced_motion` + `pointer_type` on `DeviceDescriptor` (a11y-API-backed on both platforms), riding the existing register/`update_device` path.
- [x] T054 Voice latency (near-real-time pass, worker-side, adversarially verified): endpoint silence 1.28s → 960ms default + `VOICE_ENDPOINT_SILENCE_MS` knob (clamped 320–2560ms; NOT spec-pinned — verified); trailing-silence trim before batch ASR (VAD-verdict-based, 128ms tail, fail-closed); bounded LRU TTS cache for the closed server-owned phrase vocabulary only (FR-045 carve-out recorded in spec.md; drift-pinned to `APPROVED_PHRASE_TEXT`; ≤6MiB). Voice lanes: 340 + 290 + 484 passed. TTS-prefetch design (hides 0.4–2s per quantum) written up + GO-with-flag-off recommended but DEFERRED to a follow-up (1.5–2 day cost; design in the 2026-08-03 workflow journal).
- [x] T055 MARKETING_VERSION 1.1 → 1.2 at all ten pbxproj sites (Apple auto-release fires on this merge).
- [ ] T056 OPERATOR: sandbox voice remediation (stale VOICE_CONTROL_SECRET recreate per voice-prod-diagnosis.md). **UPDATE 2026-08-04 (Windows box): the speech models are BACK on api-llm-factory** — `/v1/models` lists `Systran/faster-whisper-large-v3` (ASR) and `speaches-ai/Kokoro-82M-v1.0-ONNX` (TTS) again, and a locally-started voice worker preflighted `ok attempt=1` and was admitted, so the external blocker is gone. If the sandbox worker container kept running since 2026-08-03, T031's 5→60s preflight retry should have self-healed it — remaining sandbox suspect is the stale `VOICE_CONTROL_SECRET` (recreate, not restart). Sandbox host SSH (128.163.202.61) times out off-VPN from the Windows box; needs VPN or operator hands.
- [ ] T057 OPERATOR: production `.env` value diff (mac-agent-notes task A final leg) — no production env copy exists on this Mac; requires sandbox host access. Local side complete: FF parity proven (only deliberate unsets missing), LiveKit IPs corrected to this Mac's LAN address, `USE_MOCK_AUTH=false`, `FF_CONVERSATIONAL_VOICE` declared, recreate + `printenv`/readyz verified.

## Follow-ups from the pre-release adversarial review (2026-08-03, all verified real, none release-blocking)

- [ ] R-1 Unread badge provenance: the collapsed-drawer badge uses a +1-delta heuristic; a multi-doc-card `ui_upsert` under-counts, a reasoning row + narrative in one reply over-counts, a chat switch to exactly-one-more-turn phantom-badges. Structural fix = reducer-level live-vs-hydration stamping (`reduceChatLoaded` vs `reduceUiRender`/`reduceUiUpsert`) + exclude `role=="reasoning"`. Cosmetic severity.
- [ ] R-2 Collapsed floating bar can exceed the container below ~380pt usable height (iPhone Max landscape + open drawer + chips + step trail): clamp `drawerHeight` against `containerSize.height` in the next Apple layout pass (graceful clip today; no layout cycle — closed-form height).
- [ ] R-3 Default-mic placeholder has no failure state: against a server that never sends `composer_state` (deployment skew only), "Checking voice availability…" persists forever; add a ~10s degrade to "Voice unavailable" (ChatView + WatchChatView).
- [ ] R-4 `statusMessage` off/ready gate keys on state, not content — align to Windows' content-keyed quiet-set so a future server message on off/ready isn't Apple-only-dropped (no current trigger; server sends no message on that path today).
- [ ] R-5 `speaker-stop` vs `speaker-muted` render near-identical glyphs (`speaker.slash.fill` vs `speaker.slash`) while both visible during assistant speech; pick a distinct symbol for speaker-stop across ChatView + WatchChatView maps (coordinate with the parity checklist).
- [ ] R-6 TTS prefetch (hides 0.4–2s of dead air per recap quantum): full contract-safe design + GO-with-flag-off recommendation preserved in the 2026-08-03 voice-latency workflow journal (`wf_c468c305-49c`); ~1.5–2 days incl. adversarial worker tests. Do as its own feature.
- [ ] R-7 Composer squeeze during an ACTIVE voice session (Apple): 4 inline session controls + paperclip + send at the 320pt rail minimum leave the text field ~80-160pt (placeholder wraps). Transient (user is speaking) and the field grows vertically, but consider web's narrow-rail trick (input takes its own full row) for the rail composer.
- [ ] R-8 **Voice talks over itself (owner-observed live, 2026-08-03)**: overlapping assistant speech across announcement lanes/turns. Mechanism per the 065 architecture: announcements are serialized per turn-recap, but ack/progress/greeting lanes and RAPID SUCCESSIVE TURNS each publish their own LiveKit track and the client plays every authorized announcement — nothing serializes ACROSS lanes/turns. Once outputs overlap, the worker capture fence (scoped to its own playout) lifts while another lane still plays, so the mic re-transcribes assistant speech as user turns (observed ghost turns "Bye-bye." / "You're gonna." = fragments of its own farewell). Fix shape: a per-session GLOBAL announcement queue (one audible track at a time, later lanes wait or supersede) + capture fence spanning ALL active playouts + widen the self-speech dedup window; coordinate with the deferred TTS-prefetch design (R-6) since both touch the announcement runner. High priority for voice UX.
