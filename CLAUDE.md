# AstralDeep Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-07-10

## Active Technologies
- Python 3.11+ (backend), TypeScript 5.x (frontend, Vite + React 18) + FastAPI, websockets, existing OpenAI-compatible LLM client (`_call_llm`); React 18, Tailwind, framer-motion, lucide-react, existing `fetchJson` helper. **No new third-party libraries** (Constitution V). (013-agent-visibility-tool-picker)
- PostgreSQL — existing tables (`agent_ownership`, `agent_scopes`, `tool_overrides`, `chats`, `user_preferences`); schema delta: `chats.agent_id`, `tool_overrides.permission_kind`. Idempotent auto-migration per Constitution IX. (013-agent-visibility-tool-picker)
- Python 3.11+ (backend), TypeScript 5.x on Vite + React 18 (frontend) + Backend — FastAPI, websockets, the existing OpenAI-compatible client used in `_call_llm`. Frontend — React 18, Tailwind, Framer Motion, sonner (already present). (008-llm-text-only-chat)
- Postgres (existing). Tutorial steps live in the `tutorial_step` table seeded by [backend/seeds/tutorial_steps_seed.sql](../../backend/seeds/tutorial_steps_seed.sql); no new tables. (008-llm-text-only-chat)
- TypeScript 5.x (frontend), Python 3.11+ (backend — no backend code change expected for this feature) + Vite + React 18, framer-motion, sonner, existing `fetchJson` helper in `frontend/src/api/feedback.ts`. No new dependencies. (010-fix-page-flash)
- N/A — feature is pure frontend behavior; no schema changes. (010-fix-page-flash)
- Python 3.11+ (backend; per Constitution Principle I and existing `grants` agent module). + Existing — `shared.base_agent.BaseA2AAgent`, `shared.protocol.MCPRequest/MCPResponse`, `shared.primitives` (Text, Card, Alert, Table, List_, Tabs, MetricCard), `agents.grants.mcp_server.MCPServer`, `agents.grants.mcp_tools.TOOL_REGISTRY`, `agents.grants.caai_knowledge`, the project's existing OpenAI-compatible LLM client (used by `_call_llm`). **No new third-party libraries** (Constitution Principle V). (011-nsf-grant-agent)
- N/A. No new tables, no schema changes. Cross-session memory inherits the existing `grants` agent's posture per Clarifications Q3. (011-nsf-grant-agent)
- Python 3.11+ (backend), TypeScript 5.x (frontend, Vite + React 18) + FastAPI, websockets, the existing OpenAI-compatible LLM client (`_call_llm`); React 18, Tailwind, framer-motion, sonner, the existing `fetchJson` helper. **No new dependencies** (Constitution V). (012-fix-agent-flows)
- PostgreSQL — existing `draft_agents` and `agent_ownership` tables. **No schema change required**; FR-016/FR-017 are satisfied by existing columns plus the existing `delete_draft` endpoint at [`api.py:1014`](../../backend/orchestrator/api.py#L1014). (012-fix-agent-flows)
- Python 3.11+ (backend); vanilla ES5-compatible JavaScript maintained by the orchestrator render layer (`backend/webrender/static/client.js`, no build step) + Existing only — FastAPI, websockets, `python-jose` (JWT), `cryptography` (Fernet, already used by `offline_grant.py`), httpx/requests (existing Keycloak calls), astralprims (first-party, consumed unchanged per spec A9). **No new third-party libraries** (Constitution V). (028-workspace-auth-revival)
- PostgreSQL via existing `shared/database.py` `_init_db()` idempotent migrations. Deltas: new `web_session` table; new `workspace_snapshot` table; new columns on `saved_components` (`component_id`, `position`, `updated_at`); new `auth_revocation_queue` table for offline-tolerant revocation. Rollback documented in [data-model.md](data-model.md). (028-workspace-auth-revival)
- Python 3.11+ (backend); ES5-compatible vanilla JavaScript + CSS maintained by the orchestrator render layer (`backend/webrender/static/`, no build step) + Existing only — FastAPI, websockets, psycopg2, the OpenAI-compatible LLM client used by `_call_llm` (resolved through the feature-006 `llm_config.client_factory`), `shared.external_http` (egress-gated HTTP), astralprims v0.1.0 (consumed unchanged). **Zero new runtime dependencies** (Constitution V). CI-only tooling: ruff, pytest-cov, diff-cover, gitleaks action (Constitution XI carve-out, documented in PR). (029-agents-adaptive-ui-ci)
- PostgreSQL via `shared/database.py::_init_db()` idempotent startup migrations. Deltas: new `workspace_layout` table; additive `workspace_snapshot.layouts` column; one-time guarded agent-id/tool-name remap (classify/forecaster/llm_factory → ml_services); cleanup of permission rows for the six removed agent ids. Rollback documented in [data-model.md](data-model.md). (029-agents-adaptive-ui-ci)
- Python 3.11+ (production image) / 3.13 (local `.venv`) + FastAPI, websockets, psycopg2, the OpenAI-compatible LLM client (`_call_llm` via `llm_config.client_factory`), `cryptography` (Fernet, offline grants), `python-jose` (JWT), astralprims ≥0.2.0 (defines primitives), `shared.external_http`. PHI gate uses already-approved presidio/spacy/tzdata. **No new third-party runtime libraries.** (030-finish-soul-integration)
- PostgreSQL via `shared/database.py::_init_db()` idempotent, guarded startup migrations. Existing tables reused: `scheduled_job`, `job_run`, `user_offline_grant`, `memory_item`, `short_term_signal`, `user_personalization`, `consolidation_sweep`. No new tables anticipated; any column addition (if needed for notification persistence) ships as an idempotent `_init_db` delta. (030-finish-soul-integration)
- Python 3.11+ (backend runtime image; local `.venv` 3.13). Test-side package only. + Existing only — `pytest` + `pytest-asyncio` (asyncio_mode=auto), the in-process `Orchestrator` and `VirtualWebSocket` (`orchestrator/async_tasks.py`), the LLM client-factory seam (`llm_config/client_factory.py`, injected via `orch._call_llm`), `astralprims` (component type assertions), `webrender.allowed_primitive_types()` (published vocabulary), `audit` repository (`verify_chain`, `actor_principal_from_claims`), `workspace` (`live_components`, identity), `delegation.py` (RFC 8693), `attachments` store/repository, `parser_registry`/`attachment_autoparse`. External mode reuses already-present `websockets` and `httpx`/`requests`. **No new third-party runtime libraries** (Constitution V, FR-032). (032-attachment-sdui-verification)
- No schema change (FR-032). The harness reads existing tables (`user_attachments`, `message_attachment`, `chats`, `messages`, `saved_components`, `workspace_layout`, `audit_events`, `agent_scopes`, `tool_overrides`, `draft_agents`, `attachment_parser`) and writes to them only as a side effect of driving the product under **namespaced harness principals**, then tears down deletable rows. Run records are files under a gitignored artifacts dir. (032-attachment-sdui-verification)
- Python 3.11+ (production image); local `.venv` 3.13. ES5-compatible vanilla JS/CSS for the orchestrator render layer (`backend/webrender/static/`, no build step) where the slash-command UI touches the chat input. + Existing only — FastAPI, websockets, psycopg2, the OpenAI-compatible LLM client (`_call_llm` via `llm_config.client_factory`), `cryptography` (ECIES/Fernet), `python-jose` (JWT/RFC 8693), `astralprims` (consumed unchanged), `shared.external_http` (egress-gated HTTP), and the existing `audit`, `tool_permissions`, `tool_security`, `knowledge_synthesis`, `agentic_creation`, `concurrency_cap`, `stream_manager`, `job_poller`, and `agent_runtime` modules. **No new third-party runtime libraries** (Constitution V). (040-inprocess-agents-skills-commands)
- PostgreSQL via `shared/database.py::_init_db()` idempotent, guarded startup migrations. Deltas: new `agent_trust` table (per-agent safe/owner-approval record); a one-time guarded cleanup of orphaned permission/credential/chat rows for retired `etf-tracker-1-1`. No schema needed for skill packs (filesystem, committed) or the curated slash-command set (in-code registry). Rollback documented in [data-model.md](data-model.md). (040-inprocess-agents-skills-commands)
- Python 3.11 (backend, Docker image); Python 3.10+ + PySide6 (Windows client); Kotlin 2.0.21 + Jetpack Compose (Android); ES5 vanilla JS (web render layer) + Existing only — FastAPI, websockets, psycopg2, astralprims (unchanged); PySide6/QtCharts + stdlib urllib (Windows); OkHttp/kotlinx.serialization/AppAuth/Coil (Android). **Zero new third-party runtime deps; one removal** (unused navigation-compose). (044-native-client-parity)
- PostgreSQL via `shared/database.py::_init_db()` idempotent guarded migrations. Delta: exactly one additive nullable column `auth_revocation_queue.client_id TEXT` (native-logout revocation retries; rollback documented in data-model.md). (044-native-client-parity)
- Python 3.11 (backend, production image; local `.venv` 3.13); ES5 vanilla JS + CSS (webrender static, no build step); Python 3.10+/PySide6 (windows-client); Kotlin 2.0.x + Jetpack Compose (android-client) + Existing only — FastAPI, `websockets`, psycopg2 (its bundled `psycopg2.pool` is newly *used*, not newly *added*), the OpenAI-compatible client via `llm_config/client_factory.py`, `python-jose`, `cryptography`, astralprims (unchanged), presidio/spacy (existing, pre-warmed). **Zero new third-party runtime dependencies.** (052-perf-comment-hygiene)
- Python 3.11 (backend, production image; local `.venv` 3.13); ES5 vanilla JS/CSS for the web render layer (`backend/webrender/static/`, no build step); PySide6/Python (Windows client); Kotlin 2.0.21 + Compose (Android); Swift/SwiftUI (Apple) + Existing only — FastAPI, `websockets`, psycopg2, the OpenAI-compatible client (`openai` package, already present) resolved through `llm_config/client_factory.py`, `cryptography` (Fernet, already used by the credential manager / session store), `astralprims` (consumed unchanged), existing `audit`, `chrome_events`, `scheduler`, `agentic_creation` modules. **Zero new third-party runtime dependencies** (Constitution V). (054-byo-llm-setup)
- Python 3.11 (backend, production image; local `.venv` 3.13); + Existing only — FastAPI, `websockets`, psycopg2, the (055-uniform-artifacts)
- Python 3.11 (backend, production image; local `.venv` 3.13); ES5 vanilla JS/CSS + server-rendered HTML for the authoring chrome surface (`backend/webrender/chrome/surfaces/authoring.py`, no build step); Python 3.10+/PySide6 (windows-client — the desktop host that runs the user's agent as a child process) + Existing only — FastAPI, `websockets`, psycopg2, the OpenAI-compatible client via `llm_config/client_factory.py` (BYO codegen + phase drafting run on the OWNER's LLM, `_call_llm_json`), `astralprims` (consumed unchanged server-side; added as a CLIENT-only dependency to `windows-client/requirements.txt`/`AstralDeep.spec` so the delivered bundle's one third-party import resolves inside the frozen worker — same posture as `sigstore`). Reuses feature 057's `user_agent` registry, `FF_BYO_AGENTS`, the baked agent constitution + loader, and the deterministic Analyze gate. **Zero new orchestrator/product-image runtime dependencies** (Constitution V). (057+058-byo-client-agents)
- PostgreSQL via `shared/database.py::_init_db()` idempotent guarded migrations. Reuses feature 057's `user_agent` table + the `draft_agents` authoring columns (`phase`, `clarify_answers`, `plan_json`, `analyze_result`, `constitution_version`); the Tasks artifact rides `plan_json["tasks"]`. No new tables, no new columns — `SCHEMA_REVISION` untouched. (057+058-byo-client-agents)

- Python 3.11+ (backend), TypeScript 5.x on Vite + React 18 (frontend) (002-file-uploads)

## Project Structure

```text
backend/        # Python FastAPI backend (orchestrator, agents, webrender, tests)
windows-client/ # Standalone Windows desktop client (PySide6/Qt Widgets, PyInstaller)
android-client/ # Native Android SDUI client (Kotlin + Jetpack Compose, feature 041)
apple-clients/  # Native Apple SDUI clients — iOS, macOS, watchOS (Swift/SwiftUI, feature 051)
docs/           # Additional documentation
specs/          # Feature specification documents (speckit)
```

Note: the React/Vite frontend was removed in feature 026; the UI is now
server-driven from the backend (`backend/webrender/`). An untracked
`frontend/node_modules/` artifact may linger locally but is gitignored and
contains no source.

## Commands

Everything runs in the `astraldeep` container (no host venv; backend needs py3.11):

```bash
docker compose up -d                                   # postgres + astraldeep
docker cp <file> astraldeep:/app/<repo-rel-path>       # sync one edit (source is baked)
docker exec astraldeep bash -c "cd /app/backend && python -m pytest -q"   # full suite
ruff check .   # lint from the REPO ROOT on the HOST/CI — ruff is NOT in the image and ruff.toml is not baked (matches ci.yml)
```

Dev posture: `.env` must have `ASTRAL_ENV=development` (unset == production fail-closed). Production: see [docs/production-deployment.md](docs/production-deployment.md) — runtime-only secrets (never baked into the image), TLS proxy with `FORWARDED_ALLOW_IPS`, `/healthz`+`/readyz` probes, boot gate refuses missing/placeholder secrets.

## iOS Simulator Automation Guidelines

Always prefer the installed MCP tools (`ios-simulator-skill`, `xclaude-plugin`/`mcp__xcode__*`)
over raw `xcrun simctl` shell commands.

- To check the UI layout, ask the tool to take a screenshot or read the accessibility tree.
- To interact, use semantic element labels rather than guessing pixel coordinates.

Scope: this governs DRIVING a running app on the iOS Simulator. It does not replace
`xcodebuild` for build/test (`apple-clients/AstralApp`, schemes `AstralApp`/`AstralWatch` —
see [specs/060-runtime-reliability-hardening](specs/060-runtime-reliability-hardening)), and it
does not apply to the **macOS** client, which is not a simulator (drive that app directly).
Apple sims have no dev-token path — sign-in is real PKCE by design, so a human signs in once.

## Code Style

Python 3.11+ (backend); ES5 vanilla JS maintained by the orchestrator render layer (`backend/webrender/static/`, no build step): Follow standard conventions

## Recent Changes
- 077-agent-authoring-ux: **Create your own agents and skills — the easy path.** Walk-through findings: "My agents" named three things, four incompatible create paths, eleven actions to a first personal agent with no express lane, a guaranteed first-time dead-end (`host_online` was tunnel-only, so a healthy signed-in desktop read as offline), the desktop client invisible (no list/status/Stop for the LLM-written child processes), Clarify's gate naming a button that did not exist, silent Analyze invalidation, seven Generate outcomes, skills with no UI (and "Skills" already meaning permission toggles), fixed slash commands. **Express lane** (`orchestrator/agent_quick_create.py`): one description → the SAME 058 gated session (Specify → Clarify → Plan → Tasks → Analyze → Generate) as a background pipeline with a live step checklist re-rendered for the initiating socket while it is open (`chrome_events.open_surface_for`); stops ONLY at Clarify with questions (resumed through `advance`, the hard gate) — an Analyze refusal ends as `failed` with the violations and **Fix in the editor**; outcomes are three: delivered / waiting for your desktop (**Resend**, no model call) / failed. Runs in a detached context (`orchestrator/detached_context.py`, shared with 076's continuation — the orchestrator runs as `__main__`, so the ContextVar is resolved from the running instance's module). **Presence** (`agent_authoring.host_presence`): host sockets ∪ tunnels, labelled by the 076 computer-host name or platform+version; the surface says what to do when nothing is connected. **Skills** (`orchestrator/user_skills.py`, `FF_USER_SKILLS` default on): markdown files in the authored-pack format under `<knowledge>/user_skills/<owner-hash>/<slug>.md` (bind-mounted; no schema change), ≤20 per owner, name / optional `/command` / applies-to (every chat or agent ids) / instructions; always-skills lead every turn's skill digest and agent-scoped ones join that agent's pack (`skill_packs.build_skill_digest(orch=, owner=)`); `/command text` expands to the skill's instructions + the text (`slash_commands.expand_message(message, user_skills)`; curated names reserved; `/help` lists them); `GET /api/chrome/commands` feeds the web typeahead, refreshed from the surface's `data-astral-commands` on every modal render. **Surface** `agent_authoring` retitled **My agents & skills** (menu item; "My skills" when only skills are on — `skills_enabled` availability input): desktop status · quick create · run cards · agents · editor sessions · skills · "Advanced: build it step by step"; step editor says **Find open questions**, warns when a passed Analyze no longer covers the artifacts and replaces Generate with Re-run Analyze. **Naming**: Agents & permissions' first tab is **Owned by me**, its empty state points to My agents & skills, the Drafts form is titled as a server-side agent needing admin approval. **Windows client**: `ByoAgentHost.inventory()` + a client-local **Agents on this PC** dialog (THIS PC heading in Settings; status online/starting/offline, pid, revision; Stop through the host's own `stop`; Open folder; 2 s refresh). Docs: [docs/your-own-agents-and-skills.md](docs/your-own-agents-and-skills.md). Tests: `tests/test_authoring_ux_077.py` (pipeline, Clarify stop/resume, Analyze refusal, waiting/resend, presence, skill store, digest/expansion, surface, push gating), `windows-client/tests/test_local_agents_077.py`. Zero new deps; no schema; no new wire frames. **PROVEN LIVE 2026-09-02 on the rig** (local orchestrator, mock auth, Windows client from source on RyzenRoll): from the web client, one description + five answered questions → the checklist ticked through to *Running on RyzenRoll*; the agent (Tip splitter) answered a chat request through the tunnel in ~60 ms with a canvas; it came back by itself after the desktop client was killed and relaunched; a skill with `/standup` showed up in the web typeahead as "your skill" and expanded to the three-line standup. **Eight defects the rig surfaced were fixed the same day, each pinned** — most of them in the untested-since-074 v3 personal-agent path: the static code gate flagged `re.compile` as the dangerous builtin (`_get_call_name` now matches builtins only); the Plane immutable bundle store needs `RENAME_NOREPLACE`, which the 9p bind mount cannot do (`PERSONAL_AGENT_ARTIFACT_ROOT` → a container-local path on the rig; documented); the host refused the server's pre-launch fence because `RuntimeFence.to_dict()` carries `process_id: None`; the server refused the host's first `starting` frame as a stale fence (`get_runtime_revision` admits a spawned process on a pre-launch instance); the host's exact-listing integrity check called a revision that had ever run corrupt (`__pycache__`) and **deleted it** on the next reconnect (ignored; worker runs with `PYTHONDONTWRITEBYTECODE=1`); a restarted desktop never got its agents back because nothing re-selected a host after `host_lost` (sticky re-selection in `get_selected_session_revision` and reconciliation); the host's transport guard accepted only `agent_*` frames, so the child's `mcp_response` — the tool result — was refused on the way out and every call timed out (`mcp_response` joins the vocabulary); the Plan prompt got no tools for a pure-computation agent (the prompt now says the tools ARE the functions the owner calls; one draft retry). Diagnostics: `ASTRAL_CLIENT_LOG_LEVEL=INFO` on the client; every host-side drop and every skipped inventory entry is logged with its reason. **NOT live-verified**: the Windows *Agents on this PC* dialog (offscreen-tested only), Android/Apple rendering of the new home view.
- 076-remote-computer-control: **Drive your own Windows PC from your phone (Claude/ChatGPT-style computer use), flag `FF_COMPUTER_USE` default OFF.** New in-process built-in `computer-use-1` (24-verb closed set: session / observe / input / consequential tiers — **no arbitrary-shell verb** (D8: `run_command`-shaped tools are hard security-blocked by `tool_security`, Constitution VII; keystrokes into a terminal are the approval-gated step, unlocked for 3 min by an approved `confirm_action`/shell `open_app`), `orchestrator/computer_use_policy.py` is the single source the registry, gate and contract test share). Transport = the desktop client's EXISTING authenticated UI socket: additive `register_ui.computer_host` descriptor (strict, fail-closed — a malformed descriptor costs eligibility, never the session), `computer_request` push ↔ `computer_response` ui_event correlated by request id (`orchestrator/computer_hosts.py`, owner-scoped `(owner_sub, host_id)` registry, per-owner rate cap shared with the 058 tunnel, 4 MiB size cap, presence pushes `computer_host`), and `computer_event` for announce/withdraw/paused/resumed/stopped/heartbeat. Sessions (`orchestrator/computer_sessions.py`): started only by a live human, ack-gated by the host's first heartbeat, one controller at a time (take-over when the previous controller socket is gone), paused by local input, ended by user/local stop, consent revocation, host loss, idle/max/silence; every transition audited (`agent_lifecycle` · `computer_session.*`) and pushed as `computer_session`. **Consequential verbs (`write_file`/`delete_path`, a shell `open_app`) and the model's own `confirm_action` ride the 063 durable proposal card on every reach — delivered as the call's RESULT (canvas + transcript, replaced in place on decision), the model is told to end its turn, a repeat reach re-uses the card, and Approve re-dispatches the verb then resumes the task with a continuation turn** — `remote_confirmation.py` gained a per-agent policy table (063 behaviour byte-identical, pinned); computer-use-1 unattended ⇒ only `list_computers`. **Vision**: a tool result's `_images` tier becomes image content parts in a user message after the tool messages (spotlighted as untrusted; only the newest 3 stay as images); a provider that rejects images gets ONE text-only retry and is remembered per (endpoint, model); compaction charges an image part a fixed cost. Surface `my_computers` ("My computers", web `render()` + native `components()`, zero per-client code) lists hosts + sessions with Control/Pause/Resume/Stop/Forget, and offers the **consent switch only to a host-capable desktop** as the client-local action `computer_host_consent` (the `attach_existing` precedent). **Windows client** (Projection `windows-client/`): `win_agent/computer_use.py` executor (QScreen JPEG capture with capture geometry for coordinate mapping, ctypes SendInput/SetCursorPos, EnumWindows, clipboard, bounded files/commands) + `astral_client/remote_control.py` (persistent consent + stable host_id, always-on-top banner with Pause/Stop, GetLastInputInfo presence ⇒ pause, heartbeat, one-at-a-time worker execution, socket loss ends the mirrored session). **Android**: `image` decodes data: URLs; `computer_session`/`computer_host` refresh an open My computers surface. **Apple**: manifest tables only (`.ignored`), Mac follow-up. Manifest 72 pushes / 109 actions / 12 additive fields; composition repinned to Projection `719e833`. Zero new deps; no schema change (D7: durable host inventory deferred). Docs: [docs/remote-computer-control.md](docs/remote-computer-control.md). **PROVEN LIVE 2026-09-02 from BOTH controllers** (local orchestrator, mock auth, Windows client from source on RyzenRoll): web — consent switch → host announce → session + banner → approval card for `open_app powershell` → Approve → continuation turn → `type_text Get-Date` under the terminal grant → Enter → screenshot → GLM-5.2 read the printed date back correctly; Android emulator (real Keycloak sign-in; the Windows client ran under a mock-auth JWT carrying the owner's `sub`) — My computers refreshed live on the presence frame → Control this computer → "take a screenshot of my PC" → accurate desktop description with the screenshot rendered on the phone → Stop from the phone. **Nine rig-found defects fixed the same day, each pinned by a test**: no `run_command` (D8); card-as-result; refusal-loop guards; the host's own focus ALT-tap read as local input + a remote resume never re-baselined the presence detector (session re-paused forever); `wait` refused while paused; the continuation turn inherited the approval click's execution fence — and the first fix cleared the WRONG ContextVar because the orchestrator runs as `__main__` (`import orchestrator.orchestrator` is a second module instance; resolve the var from the running instance's module); an approved verb that could not be attempted (PC paused) lost its single-use approval (180 s retry grace for one identical retry); terminals are detected by console-host window CLASS, not image name (`python.exe` as a GUI is not a terminal); Windows top-bar icons are the web SVG glyphs (the ✨ emoji rendered smeared). **NOT live-verified**: Swift changes are uncompiled; the frozen Windows EXE (QtSvg icon path) is untested; no staging deployment.
- 2026-08-23-production-enablement: **Sandbox production brought to full posture + LETS Phase B live in shadow.** (1) Defects fixed behind flags that were then ENABLED: `shared/external_http.request` sent `Authorization: Bearer ` (empty) on keyless fetches (Wikipedia 400 on every `summarize_url`); DuckDuckGo's 202 bot-challenge page was parsed as "no results" (now a typed refusal + one retry via `lite.duckduckgo.com`); `start.py` Popen'd `agents/tests/test_remote_compute_agent.py` as an agent and double-started `remote_compute` (in-process + :8004); junk directory-name `agent_ownership` rows swept at boot (`LEGACY_DIRECTORY_AGENT_IDS`, exact ids only); `SPEACHES_URL` retired (`speech_server_available` now derives from the voice runtime). (2) Flag hardening (each audited, fixed, adversarially reviewed): **taint** — `fetch_page`/`web_search` are no longer sinks (read-only GETs behind the SSRF guard) and values typed verbatim by the user are user-supplied; denials now leave an `agent_tool_call` audit row. **MoA** — real difficulty gate (`MOA_DIFFICULTY_THRESHOLD`), judge-ranked winner, panel runs BEFORE the supervisor so a block is final, skipped on streamed/background/MCP turns. **A2A server** — bearer gate before SDK dispatch, per-caller task scoping (the SDK's InMemoryTaskStore leaked every caller's tasks to anonymous `ListTasks`), public card = safe first-party catalog only, token validation at least as strict as the web gate (iss/azp/aud/role, delegation tokens refused, shared JWKS cache). **Scheduler** — token endpoint derived from `KEYCLOAK_AUTHORITY`; bounded genuine-failure retries (`SCHEDULER_MAX_ATTEMPTS`) with a 10× claim-loop ceiling that never blames the job; stale-backlog guard (`SCHEDULER_STALE_GRACE_SECONDS`) that never skips one-shots, always runs the last catch-up, and notifies once per backlog. **MCP** — realm `user`/`admin` role required, honest "signing key unset" refusal, tool name is the intent over MCP. **Router** — audit rows record the routed model, probe cache keyed per routed/escalated model. **Adaptive objectives** — designer receives the connecting device. `FF_HITL_HIGHRISK` turned OFF (owner decision: no client has a confirm affordance). (3) **LETS Phase B**: one warden (`astral-lets-warden`, `generic-production` provider + software Ed25519 signer helper, mTLS, signed single-warden manifest; GHCR package private → local build of the signed commit) runs beside the stack; orchestrator wired in `LETS_MODE=shadow` with **per-request EdDSA identity** (`MintingLETSClient`, `LETS_IDENTITY_*`; Keycloak is not usable as the LETS identity without new code — RS256, no `nbf`); **server_dynamic runtimes receive their authority descriptor + per-runtime audience/roots at spawn** (`dynamic_runtime_authority.py`, retention sweep `LETS_EXECUTOR_RUNTIME_RETENTION_DAYS`); `/readyz` gains a live-probed `lets` entry (`lets_probe.py`, 503 only for enforce+blocked) and admin `GET /lets/health`. Operator guide: [docs/lets-external-warden.md](docs/lets-external-warden.md); Compose template `deploy/lets/docker-compose.lets.yml`. (4) Apple: `apple-release.yml` now attaches the validated `.ipa`/`.pkg` + `SHA256SUMS` to the GitHub Release from a separate `contents: write` job; App Store Connect had already approved 1.3, so Projection bumped to `MARKETING_VERSION 1.4` and the tag is `apple-v1.4`. (5) Repo hygiene: `event_loop_guard` tolerates a missing `astralplane` (the tooling CI lane was red since the 074 merge); four accidentally tracked `backend/knowledge/*` synthesizer outputs untracked. **Known open items**: voice waits on the UK LLM-factory audio endpoints (HTTP 500 upstream); MCP needs the Keycloak `astral-mcp` client; the old scheduled job needs re-consent via chat (no offline grant exists); LETS `enforce` flip pending a shadow soak + the T231 fault drills; GHCR `lets` package visibility.
- win-agent-inbound-auth: **The client-hosted Windows tools listener is no longer unauthenticated, and `AGENT_API_KEY` now authenticates BOTH directions.** `win_agent` served `GET /.well-known/agent-card.json` and WS `/agent` on `0.0.0.0:8771` with NO inbound auth, and `_agent_ws` called `ws.prepare()` FIRST and then pushed the register frame — which *contains* `AGENT_API_KEY` — to whoever connected, before reading a byte. Any host reaching the port could harvest the key and drive `read_file`/`write_file`/`edit_file`/`run_command` on the user's PC. Latent, not live: the listener starts only under a `legacy_tools: managed_api_key` profile and both shipped profiles set `disabled`. **Fix** (new `backend/orchestrator/agent_peer_auth.py` + `backend/shared/ws_compat.py`): the orchestrator presents the same shared secret as **`X-Astral-Agent-Key`**; the agent verifies with `hmac.compare_digest` BEFORE `ws.prepare()`, using `headers.getall()` (duplicates reach the handler and `.get()` returns only the first, so a caller could pair a wrong value with the real one) and `isascii()` first (`compare_digest` raises `TypeError` on non-ASCII, which inside a handler is a 500, not a refusal). `Authorization` was NOT reused — it already carries the per-call RFC 8693 delegation token. **The listener refuses to exist without a usable key** (16+ ASCII, no placeholder), enforced in `make_app` so it covers `python -m win_agent.agent`, the GUI and the screenshot/verify harnesses; **no `ASTRAL_ENV` dev carve-out on the client** — the backend's carve-out is nested inside "no key configured", and the safer answer to that case is not to listen. `/health` stays open; the bind stays `0.0.0.0` (a containerized orchestrator reaches the desktop at `host.docker.internal`, which can never reach a loopback bind) with a new `ASTRAL_AGENT_BIND` knob and a non-loopback WARNING. **Two controls gate the outbound key, not one**: the destination must be operator-declared (loopback / Docker host aliases / `AGENT_KEY_TRUSTED_HOSTS` — deliberately NOT `A2A_EXTERNAL_AGENTS`, a *discovery* list whose entries never consented to hold the fleet secret) AND the peer must first answer an UNAUTHENTICATED probe with the `AstralAgentKey` challenge, so the key is never on the opening request and an unrelated dev server on a trusted port never sees it. This matters because `register_external_agent` takes a raw user-supplied URL with no egress validation and this key can register or overwrite ANY agent id. Redirects are refused (`allow_redirects=False`: aiohttp forwards headers across a redirect even cross-host — verified — which would walk the key around the destination gate); `wss://` is preserved (the old inline expression hardcoded `ws://` and stripped `https://`); both `httpx` card resolvers gained `follow_redirects=False` + a 5 s timeout; a `logging.setLogRecordFactory` hook redacts the key from every record because `websockets` dumps handshake headers at DEBUG (a logger-level `Filter` does NOT see records from child loggers such as `websockets.client`). **Verified live** against the running orchestrator: probe→challenge→credential→card 200→WS 101→`register_agent`→`tools/list`; mismatched key → 401 + an actionable WARNING; a full DEBUG discovery emitted 0 occurrences of the key. windows-client **744 passed / 6 skipped**; 40 new backend tests. **NOT verified: the frozen EXE** (new imports and the refusal branch are source-tested only; `main()` now guards `sys.stderr is None`, which a windowed build has, and `AstralDeep.spec` now `collect_submodules("websockets")` instead of relying on incidental bytecode) **and no staging deployment.**
- 064-mcp-2026-07-28-decision: **Phase A and the flag-gated Phase B server are CODE-SHAPED, not proven live.** The internal MCP envelope now tolerates additive fields, fails malformed correlated frames promptly, carries protocol/capability/peer metadata and `result_type`, enforces result-xor-error, and validates bounded offline JSON Schema 2020-12 declarations at registration before model-only adaptation. `FF_MCP_SERVER` remains default-off; when enabled it mounts stateless POST `/mcp` plus protected-resource metadata, requires a dedicated issuer-checked `astral-mcp` audience and header-only bearer scopes, projects the same per-user tool set chat can use, routes calls through the full authorization/delegation/audit stack, refuses remote-compute destructive calls before transport, and returns deterministic MCP-rendered plus structured content. The endpoint has its own credential-free CORS policy, 1 MiB body cap, MCP admission class, cancellation handling, redacted low-cardinality observability, and repeat-safe schema revision `064.001`. MCP Apps and MCP Tasks are deliberately **not adopted**; `subscriptions/listen` is only an empty-set acknowledgement and no notification type is advertised. Local evidence: Python 3.11 MCP suite 134 passed; prior unchanged production-code candidate evidence remains 4915 passed/24 skipped for the backend root lane, 1605 passed/7 skipped for explicit nested modules, Android unit gates green, Windows feature-focused tests 39 passed/1 skipped, and changed-line diff coverage is 90%. **Not proven live:** real staging Keycloak/worker exercise, a real third-party `2026-07-28` host, and Apple gates remain outstanding; do not call Phase B shipped, deployed, or released.
- 064-followups-mcp-upgrade: **Permission-baseline defect fixes + the MCP `2026-07-28` conformance read.** Four latent defects from the follow-ups register, all found earlier and unfixed, plus one decision artifact. (1) **`backfill_per_tool_rows` no longer freezes a denial over a baseline** (`tool_permissions.py`): it used to materialise `enabled=False` per-(tool, kind) rows for a user with NO `agent_scopes` rows, and those explicit rows outrank step 1 of `_resolve_tool_allowed` — so merely *reading* the per-tool permissions endpoint (`api.py`) permanently stripped feature 040's safe deny→allow flip and the 057/058 owned-user-agent default, silently disabling every built-in agent for a user who had granted nothing and revoked nothing. Carry-forward is now per scope and only where an explicit row exists to carry (regression proven: 3 of 4 new tests fail on the pre-fix code). The 052 boot migration `_migrate_backfill_tool_kinds_052` was audited and is sound — it copies only legacy `enabled=FALSE` rows, which already blocked. (2) **Machine-turn consent reads the effective baseline** (`chain_authority.derive`, `scheduling_chat` capture + card): all three read raw `agent_scopes`, so a safe-baseline user's scheduled job captured an EMPTY consented list and its approval card promised "no scopes yet" — for the post-040 default population. All three now use `get_enabled_scope_names`, the same predicate the dispatch gate applies (pre-fix, such a run skipped `empty_scopes`; proven). (3) **BYO host-compat gate covers dynamic imports** (`agent_validator.disallowed_imports`): `__import__("requests")` / `importlib.import_module(...)` reach the owner's interpreter exactly like a static import but a statement-only AST walk never saw them — the bundle died as an unexplained host silence-timeout instead of a generation-time error. Literal names resolve against the same allowlist; a computed name is refused as uncheckable. Not a security fix (validation never executes code). (4) **Stale scope docs corrected** — `tool_permissions` module + class docstrings and `protocol.AgentSkill.scope` all said "four scopes" above a six-element `VALID_SCOPES`, and the module docstring's "all scopes DISABLED by default" no longer described the effective decision. (5) The register's **"feature-030 enable affordance is structurally unreachable" entry is OVERSTATED** — verified empirically against the live DB: it is unreachable for *fresh* users (correctly, since 040 makes their agents work) but still reachable and honest for explicit opt-out, `FF_SAFE_AGENTS` off, and safe-but-private catalogs. Code was left working; the stale fresh-user framing in the docstrings was corrected and the real reachability is now pinned by tests. **MCP `2026-07-28`**: the published revision was read against `main@90474ef` — **zero occurrences of every wire construct it adds, changes, or removes** (incl. the four majors the RC summary omitted: required `server/discover`, `subscriptions/listen`, `ping`/`logging/setLevel` removal, SSE-resumability removal), because Astral carries MCP as a message *shape* over its own A2A/loopback transport and exposes no MCP endpoint. **Nothing breaks; no migration.** Verdicts recorded in [specs/064-mcp-2026-07-28-decision/spec.md](specs/064-mcp-2026-07-28-decision/spec.md): **MCP Apps → DIVERGE** (it ships sandboxed HTML in an iframe; Astral ships a declarative vocabulary rendered per target — the iframe is a hard reach ceiling for the PySide6/Compose/SwiftUI/voice clients, and the security models are opposites: contain-untrusted-code vs admit-no-code); **Tasks → DIVERGE + cite the convergence** (063's `tracked_job` independently arrived at the same design, and is stronger at `unconfirmed` and at consent, where MCP's `input_required` is a status rather than an authorization artifact); **authorization → already conformant** (RFC 9207 `iss` binds MCP *clients*; Astral is not one); **JSON Schema 2020-12 → no-op** (the change loosens, not requires). One trigger would make the whole revision live at once: exposing a conformant MCP endpoint.
- 063-remote-compute-agents: **Remote compute over SSH** — a user registers their OWN machines/clusters (e.g. a Slurm/Bright DGX) and drives them from chat (`FF_REMOTE_COMPUTE`, default **OFF**; read once at import → container recreate to toggle; flag-off is byte-identical — no agent, menu item, or surface, pinned by `test_remote_flag_off_063.py`). **Agent shape**: one in-process built-in **`remote-compute-1`** (18 verbs) unioning two risk-tiered libraries — `agents/remote_observe` (9 read verbs: machines/probe/queue/status/history/facts/directory/processes/job-output) + `agents/remote_control` (9 mutating verbs) — safe-seeded so read verbs work out of the box; SC-003's opt-in posture rides scope declarations + the per-verb destructive gate (the registry exactly equals `DESTRUCTIVE_CLASSIFICATION`, drift-pinned). **Transport** (`orchestrator/remote_transport.py`): injectable ParamikoTransport/FakeTransport; commands run as a discrete argv vector through `bash -lc 'exec "$@"' _ …` (shell metacharacters inert, FR-022); connection-time egress gate (RFC1918 allowed; loopback/link-local/metadata + IPv4-in-IPv6 encodings refused) + anti-DNS-rebind vetted-peer check; TOFU host-key pinning with RejectPolicy — a changed key is refused and ONLY the owner-scoped `chrome_machine_retrust` accepts a new identity; per-machine credentials Fernet-encrypted; consequential (non-retryable) transport timeouts convert to structured `unconfirmed`, never a retryable `timeout`. **US3 destructive confirmation** (`orchestrator/remote_confirmation.py`, enforced in the shared `_run_gate_stack` so single/parallel/hop/component paths all gate): first reach of a destructive verb → durable single-use TTL-bound owner-bound argument-fingerprint-bound proposal (`remote_operation_proposal`); a machine-class (unattended) turn is refused `unattended_refused` for EVERY mutating verb BEFORE any transport contact — including before the `upload_file` if_exists stat and before approval-marker consumption (adversarial suite: 55 tests). **US4 async jobs** (`orchestrator/remote_jobs.py` + `tracked_job`): `run_job` (inline script via SFTP) and `submit_job` (existing script) both stamp a `--comment=<nonce>` idempotency marker and INSERT a durable tracked job; an always-on read-only poller updates the same canvas card in place and `notify_on_finish` pushes a client banner; boot reconciliation resolves jobs that finished during an outage; slow/lost submit → `unconfirmed`, zero duplicate rows (20-iteration sweeps). **Surface**: `webrender/chrome/surfaces/remote_machines.py` (web `render()` + native `components()` — every native client renders it through the generic SDUI renderer, zero per-client code; `visible_when` conditional credential fields on all three native clients); handlers add/probe/delete/credential-set/credential-delete/retrust, all owner-scoped and audited (`agent_lifecycle` class, `remote_machine.*` action types, correlation_id = machine_id; secrets never in any row — sentinel-sweep test with planted-leak tripwires). **FR-015**: machine credentials destroyed on machine delete, credential delete, logout (web + native), and the account-removal hook. **Model-tier honesty**: `read_job_output` puts the bounded sanitized tail in `_data` (the LLM sees only `_data` — it was previously blind to output it was asked to interpret and re-submitted `cat` jobs). **Apple client fix that fell out of live-verification**: the 063.2 stuck-skeleton canvas livelock was TRULY root-caused to combinatorial StackLayout flexible-space measurement over ScrollView/LazyVStack ideal-height queries (the earlier shimmer fix was necessary, not sufficient) — fixed with GeometryReader layout firewalls + concrete slab heights in `ChatView.swift`; 3/3 wedged → 4/4 clean live. **PROVEN LIVE 2026-07-27/28** against the real UK DGX from the iOS sim + macOS: register→probe ok, sbatch submit (incl. honest `Invalid account` stderr surfacing + LLM self-correction), tracked job → poller → COMPLETED → notify banner → output card; unreachable-DGX (VPN off) renders the enumerated verdict + next steps. **NOT live-verified**: staging deployment of the candidate image (T071, operator-gated); web's 2-of-9 sweep misses (pre-063, still open). Schema `063.005`; zero new runtime deps beyond the D1-approved `paramiko` (+`cryptography` pin; transitives bcrypt/pynacl/cffi). Operator doc: [docs/remote-compute-agents.md](docs/remote-compute-agents.md).
- 057+058-byo-client-agents: **BYO client-side agents** — a user authors a small personal agent that runs as an ISOLATED CHILD PROCESS on their OWN desktop and dials inward over a tunnel on the client's authenticated UI WebSocket, UNTRUSTED at the orchestrator boundary (`FF_BYO_AGENTS`, default **OFF**; read once at import in `shared/feature_flags.py`, so toggling needs a restart — no admin UI; every authoring/tunnel/lifecycle entry point re-checks `agent_authoring.byo_enabled()` so flag-off is byte-identical). **Transport (Mode 1 direct tunnel)**: frames ride an `agent_tunnel` envelope on the owner's UI socket (`ui_event` C→S / bare push S→C, `frame` a JSON string); `shared/local_transport.py::TunnelSocket` adapts it to a `.send`-shaped agent socket carrying the AUTHENTICATED owner `sub` — a small transport seam so Cresco Mode 2 can slot in later (deferred, `contracts/agent-tunnel.md`). **Owner-bound registration** (`user_agents.authorize_registration`): owner derived from the session `sub`, NEVER the card; admitted only when the `user_agent` row is owned by that sub, `status ∈ {validated,live}`, and not `revalidation_required`; reserved (`__*`)/built-in/public/other-user ids refused fail-closed; owner-scoped registry keyed `(owner_sub, agent_id)` in `orch._tunnel_sockets`, stale socket superseded on reconnect. **Honest-offline**: a tunnel drop deregisters `(owner_sub, agent_id)` + emits `agent_offline`; dispatch of an offline user agent short-circuits to a prompt honest `MCPResponse` (never a hang). **Boundary hardening (untrusted-at-the-boundary)**: per-owner ingress cap (`BYO_TUNNEL_MAX_FRAMES_PER_S`, default 50, `_tunnel_ingress_over_cap` — a flooding agent degrades only its own owner); NO delegation-token bytes / per-user secret hand-off to an untrusted tunnel agent (`_untrusted_tunnel_agent` in `execute_single_tool` mirrors the 054 in-process-only rule — the orchestrator re-authorizes every one of its tool calls through the full gate stack instead of trusting a token it holds). **Self-contained bundle** (`contracts/host-bundle.md`): `agent_generator.generate_byo_files` emits a 3-file bundle — deterministic `agent_main.py` (JSON-lines-over-stdio runner) + LLM-written `mcp_tools.py` (`TOOL_REGISTRY`, imports ONLY astralprims) + `manifest.json`; `byo_import_violations` refuses `shared`/`agents.`/`sys.path.insert`; validation is PURE-AST (`agent_validator.validate_static` — registry shape + return-format + a stdlib∪`{astralprims}` import allowlist that doubles as a host-compat gate), the exec-in-a-child validator path is DELETED, so NO user code EVER runs on the orchestrator (SC-002; the prior file-write+socket exploit now executes zero code). **5-phase authoring** (`agent_authoring.py`): Specify→Clarify→Plan→Tasks→Analyze→generate over the 057 `draft_agents` columns (`phase`/`clarify_answers`/`plan_json`/`analyze_result`), each artifact ASSISTANT-DRAFTED on the OWNER's LLM (`_call_llm_json`, never the system one) and human-editable; Clarify is a HARD GATE (won't advance with an unanswered question), the 057 Analyze gate runs BEFORE `generate_code` (a violation produces NO code), and `generation_gate` makes generate STRUCTURALLY unreachable pre-Analyze-pass (server re-check of phase + stored pass + constitution version + a fingerprint of the exact artifacts Analyze saw); the Analyze-approved tool set is persisted to `tools_spec` and enforced as a superset gate on the generated registry (`_spec_conformance_errors`, by AST). Surface: `webrender/chrome/surfaces/authoring.py` (`render()` web + `components()` native, `chrome_author_*` handlers, flag-gated "My agents" menu item). **Delivery** (`agent_lifecycle`/`agentic_creation`): a validated bundle pushes `agent_bundle_deliver` to HOST-CAPABLE sockets ONLY (`_agent_host_sockets`, additive `RegisterUI.agent_host`; a browser tab never receives code — the `no_host` branch says "open your desktop client and re-run Generate"), NEVER Popen'd on the orchestrator; on inward register `user_agents.go_live` (status='live', constitution stamped, `agent_ownership` row is_public=FALSE). **Owner tool baseline**: `tool_permissions` flips the deny→allow baseline for an agent's own owner (`_is_owned_user_agent`) so the author can actually invoke the tool they authored. **Lifecycle** (`agent_authoring`): owner-only list with derived running/offline status (`agent_status`, socket presence — never persisted); soft-delete (`user_agents.soft_delete` — stop the host child, drop routing/visibility, retain the row + audit); constitution-version re-validation (`revalidation_required` refuses routing until re-Analyze passes); NO share/publish/transfer path exists. **Windows desktop host** (`windows-client/win_agent/byo_host.py` + `byo_worker.py`): the supervisor writes the bundle under `%LOCALAPPDATA%/AstralDeep/agents/<id>/`, `subprocess.Popen`s the client re-invoked with `--byo-worker` (branched in `main.py` BEFORE Qt loads; frozen-build `_rebind_std_streams` re-opens the OS pipes because a windowed PyInstaller exe may have no `sys.stdout`), and pumps child stdout→`agent_tunnel` / inbound tunnel→child stdin as a DUMB PIPE (realpath traversal guard; `REGISTER_TIMEOUT_S` reaper for the silence-trap — a refused registration produces no NAK; rehydrate-from-disk + re-register on reconnect; terminate every child on close/sign-out/`agent_stop`); `astralprims` added to the client `requirements.txt`/`AstralDeep.spec`. **PROVEN LIVE 2026-07-14** end-to-end on a signed-in Windows host: web author → 5-phase Analyze-gated flow → generate with the owner's GLM-5.2 → static-validate → delivered to 1 desktop host → child process → registered inward over the tunnel → boundary security re-review → invoked from chat (`generate_greeting_card → ua-greeter-…`) → astralprims Card rendered. **NOT yet live-verified**: Android/Apple author-render parity (T020/T021) and host-side revise rollback (T027) are code-shaped but unproven on a live native client; Cresco Mode-2 transport is DEFERRED (T036). Reuses 057's `user_agent` registry + agent constitution + Analyze gate + owner isolation UNCHANGED; ZERO new orchestrator/product-image runtime deps (astralprims added CLIENT-side only); NO schema migration (`SCHEMA_REVISION` untouched — authoring rides existing `draft_agents` columns, the Tasks artifact rides `plan_json["tasks"]`). Operator enablement + security posture: [docs/byo-client-agents.md](docs/byo-client-agents.md).
- 056-delegated-agent-chaining: Wired feature 048's tested-but-unwired recursive-delegation mechanism into the product (zero→live call sites). **US1 interactive chaining** (`FF_RECURSIVE_DELEGATION`, default off): a tool calls `runtime.call_agent_tool(callee, tool, args)` → an `agent_hop_request` control frame routes over the agent's own channel (loopback/WS) → `Orchestrator._handle_agent_hop_request` resolves the initiator's user/chat/socket/**parent authority from its OWN `_dispatch_context` record** (never agent-supplied — closes the confused-deputy seam), charges the turn's global `ChainBudget`, mints a strictly-narrower child via `mint_child_delegation` (scopes ∩, exp ≤ parent, depth+1, actor chain to the human), verifies with `authorize_chained_tool_call`, and re-enters `execute_single_tool` so the hop faces the FULL gate stack; reserved `__`pseudo-agent ids refused pre-dispatch (meta-tool bypass structurally unreachable); empty-∩ refused fail-closed; paired `delegation.hop.mint`/`.enforce` provenance rides `audit_events` under the new `delegation` event class sharing the hop's tool-call correlation id (closes 048 T018; two-hop reconstruction pinned). First call site: `summarizer.summarize_url` hops to `web_research.fetch_page`. **US3 dispatch-path parity**: the single-path gate stack extracted into `_authorize_and_prepare`; `execute_parallel_tools` routes through it (previously dispatched UNSCOPED, skipping policy/taint/supervisor/HITL/delegation-mint/cap/hook); meta-tool parity on the parallel path; hop concurrency charges both initiating+executing slots (`_hop_cap_entries`). **US2 machine-turn authority** (dark behind `FF_SCHEDULER_EXECUTION` pending T057): one shared `orch.derive_machine_authority` → `MachineTurnAuthority.derive` (validate consent, re-check revocation at derivation, mint fresh per-run, narrow to consented∩current, else `AuthoritySkip`); `_bind_machine_turn` binds the root to the turn's virtual socket so real-agent dispatch runs delegated in production and audit rows carry `machine:<class>`+owning-human+consent_ref (were dropped as "legacy"); `OfflineGrantStore.capture` finally wired at the schedule consent card (`scheduling_chat._capture_consent`, `offline_grant_id` was hardcoded None); collapsed one-notification-per-paused-job. **US4 decomposition**: `orchestrator/subtasks.py` `delegate_subtasks` meta-tool (`__subtasks__`) spawns 2–5 bounded, isolated, budget-sliced sub-tasks returning provenance-tagged digests; MAS scan now ENFORCES on inter-agent hop results + digests (quarantine, not log-only — rides `FF_RECURSIVE_DELEGATION`); orphan cancel discards partials; progress rides existing `chat_status` (no new frame). Retired the dormant unattenuated `BaseA2AAgent.call_peer_tool`/peer-transport path (SC-010). **US5**: 047 benchmark gains chained-attack scenarios (`adapters/chained.py`) + a real-gate `ChainedDriver` (`--mode chained_real`) + `chained_delegation` envelope layer + off-vs-on comparison (`report.off_vs_on_summary`, ASR(on)≤ASR(off)). New helpers `delegation.encode_delegation_payload`/`decode_token_payload`; new module `orchestrator/chain_authority.py`. Zero new third-party deps; NO schema migration (SCHEMA_REVISION stays 055.002; trail rides `audit_events`, consent reuses `scheduled_job.offline_grant_id`, `delegation` class is a Python constant). New protocol frames `agent_hop_request`/`agent_hop_response` are agent-channel-only (ui_protocol.json unchanged, Constitution XII).
- 055-uniform-artifacts: Added Python 3.11 (backend, production image; local `.venv` 3.13); + Existing only — FastAPI, `websockets`, psycopg2, the
- 054-byo-llm-setup: Removed the operator-default LLM credential mechanism entirely (`OPENAI_API_KEY`/`OPENAI_BASE_URL`/`LLM_MODEL` env trio and all consuming code paths are now inert); per-user LLM provider config persisted server-side in new `user_llm_config` table (API key Fernet-encrypted under `CREDENTIAL_ENCRYPTION_KEY`); mandatory non-dismissible provider-setup dialog on first login across all clients (web `chrome_render`, native `chrome_surface` with `mode:"mandatory"`); server-owned provider catalog (OpenAI, Anthropic, Gemini, Grok, OpenRouter, Groq, Together, Mistral, Ollama, LM Studio, Custom); background LLM work (scheduled jobs, parser codegen, knowledge synthesis, compaction, combine/condense) moves to a single admin-managed `system_llm_config` credential with honest degradation when absent; `llm_system` chrome surface for admin configuration; `llm_gate.py` server-side gate refuses unconfigured users structurally (kill switch `FF_LLM_FIRST_RUN` for the push only); legacy `VITE_`-prefixed env aliases retired; schema revision bumped 052.001→054.001. Zero new third-party runtime deps (Constitution V).
- 052-perf-comment-hygiene: System-wide performance PR-1 — psycopg2 connection pooling (idle-retaining, IPv4-normalized) + async `Database` facade with a CI-enforced event-loop-blocking detector (empty allowlist); N+1 eliminations (recent-chats 1 query, agent detail ≤3, list ≤2, bulk attachment hydration) + per-turn permission memo; `schema_meta` revision fast path with source-hash bump guard; self-hosted fonts + per-file immutable asset versioning + Plotly lazy-load + modal skeletons; JWKS boot warm; designer upsert-first delivery (1 pass default) + `FF_LLM_STREAMING` narrative streaming over existing `ui_stream_data` frames (manifest untouched); Windows window-first launch with in-window OIDC; Android `@Immutable`/stability-config recomposition skipping; PHI pre-warm + start.py readiness polling. Kill switches: `DB_POOL_DISABLE`, `FF_LLM_STREAMING=0`, `UI_DESIGNER_MAX_ROUNDS`. Zero new third-party runtime deps. PR-2 (repo-wide comment hygiene, excludes apple-clients) pends.


<!-- MANUAL ADDITIONS START -->
## UI delivery (feature 026)

The UI is **server-driven from the backend** — there is no separate React/Vite frontend.
- **Primitives** are defined by the first-party pip package **`astralprims`** (`pip install astralprims`). Build UI with its classes (`Text`, `Card`, `Table`, …) and serialize with **`.to_dict()`** (NOT `.to_json()`, which returns a string) or `create_ui_response([...])`. The base styling field is **`css`** (not `style`).
- The **orchestrator renders** those primitive dicts to web HTML in `backend/webrender/` (pure-Python render functions, escape-by-default via `esc()`); **ROTE** (`backend/rote/`) adapts per device. New client targets register a renderer via `webrender.register_target(...)` — no change to astralprims or agent code.
- The orchestrator serves the shell + static assets on **`:8001`** (`GET /`, `/static/*`); server-side OIDC at `/auth/{login,callback,session,logout}` (`backend/orchestrator/web_auth.py`). No `:5173` static server.
- Per Constitution II (v2.0.1): **astralprims defines → orchestrator renders → ROTE adapts.**

## Chrome + agentic creation (feature 027)

- **App chrome** (top bar, static settings menu, modal surfaces) is server-rendered HTML in `backend/webrender/chrome/` — NOT astralprims primitives, and it never enters ROTE. The shell injects the role-gated top bar at `GET /` (`%%ASTRAL_TOPBAR%%`); surfaces are pushed over WS as `chrome_render {region, html}`. Surface modules in `webrender/chrome/surfaces/` export `TITLE`, `async render(orch, user_id, roles, params)` and a `HANDLERS` dict; `backend/orchestrator/chrome_events.py` dispatches all `chrome_*`/draft-decision `ui_event` actions (unmatched legacy actions still log a warning).
- **Agentic creation**: orchestrator meta-tools `create_capability`/`extend_agent` (pseudo-agent `__orchestrator__`, defined in `backend/orchestrator/agentic_creation.py`) are injected into chat tool lists when `FF_AGENTIC_CREATION` is on (default). Gap → auto-create draft (012 lifecycle) → VirtualWebSocket self-test → approve/refine/discard cards; live-agent revisions re-pass the security gate with backup/rollback. Provenance columns on `draft_agents`: `origin`, `source_chat_id`, `gap_fingerprint`, `revises_agent_id`, `self_test`.
- Audit event class `agent_lifecycle` covers the creation lifecycle; admin gating is server-side (`web_auth.session_roles` for shell render, JWT roles per handler).

## Auth lifecycle + persistent workspace (feature 028)

- **Auth gate**: `GET /` redirects unauthenticated visitors straight to Keycloak via `/auth/login?next=…` (no in-app login screen; `/auth/login` pre-flights the IdP and serves a bounded 503 retry page when unreachable). Entry is role-gated at the callback: a token with neither `user` nor `admin` role gets a 403 no-access page, no session, refresh token revoked. Sessions are durable (`web_session` table, Fernet-encrypted, `backend/orchestrator/session_store.py`) and renew silently server-side (`web_auth.ensure_session`); refresh never extends the 365-day interactive anchor (016); `/auth/session` reports `reason:'hard_cap'` at the cap and one-shot `resumed` semantics (shell injects `__ASTRAL_RESUMED__`, client echoes it into `register_ui`). WS auth failure now sends `auth_required` (client refetches `/auth/session`, then redirects). Logout revokes the Keycloak refresh token (offline-tolerant `auth_revocation_queue`) AND feature-025 offline grants. **Fail-closed posture**: `ASTRAL_ENV` unset == production — mock auth refuses to boot and agent registrations without a valid `AGENT_API_KEY` are refused (additive `RegisterAgent.api_key`, enforced in `Orchestrator.register_agent`; `BaseA2AAgent` sends it from env); set `ASTRAL_ENV=development` for local dev. Operator realm settings: [docs/keycloak-realm-settings.md](docs/keycloak-realm-settings.md).
- **Persistent workspace**: every rich component output auto-upserts into the per-chat workspace (`saved_components` + `component_id`/`position`/`updated_at`; `backend/orchestrator/workspace.py`). Identity = author `Primitive.id` → `wc_<sha1(agent|tool|params)>` fingerprint → single-source supersede (fingerprint-derived identities only — an explicit/echoed id never supersedes a different identity; new explicit ids append). REST component verbs (save/delete/combine/condense) write through `WorkspaceManager` like their WS twins; chat deletion ends any open historical view on other tabs. Wire: additive `ui_upsert {chat_id, ops:[{op, component_id, component, html}]}`, fanned out to all of the user's sockets on that chat with per-socket ROTE adaptation; full canvas `ui_render`s wrap components in `<div class="astral-component" data-component-id=…>`. `load_chat` re-hydrates the workspace and adds server-rendered `html` to component-bearing transcript messages. Per-turn `workspace_snapshot` rows power the read-only timeline chrome surface (`webrender/chrome/surfaces/workspace_timeline.py`; `workspace_timeline_mode` WS flag; mutations refused while viewing history). `component_action` ui_event re-executes a component's source tool with the chat path's permission gates and updates it in place (`table_paginate` now routes through it). Audit: `workspace.component_added/updated/removed`, `workspace.action_denied`, `workspace.timeline_viewed` (class `conversation`); `auth.logout`, `auth.token_refresh_failed` (class `auth`).

## Agent catalog, adaptive UI designer & CI (feature 029)

- **Catalog**: email_tracker, grant_budgets, grants, linkedin, nefarious, nocodb agents REMOVED (their LinkedIn OAuth REST flow in `api.py` removed too; qual_audit poisoning tests use a local malicious fixture now). classify + forecaster + llm_factory MERGED into `backend/agents/ml_services/` (agent id `ml-services-1`, shared `_wrapper.py` foundation; the 5 colliding verbs are service-prefixed: `classify_submit_dataset`, `forecaster_get_results`, …). Idempotent boot migrations in `_init_db()` (`_migrate_agent_catalog_029`) remap ownership/scopes/overrides/credentials/chats to `ml-services-1` (scopes OR-merge, overrides AND-merge, verb prefix rewrite) and delete permission/credential rows for the six retired ids. `BaseA2AAgent` gained `predecessor_agent_ids` → fallback ECIES keys so pre-merge E2E credentials still decrypt. Old-transcript `component_action`s: merged sources transparently reroute (`orchestrator.remap_merged_source`), retired sources get an audited retirement Alert (`RETIRED_AGENT_IDS`).
- **New plug-and-play agents**: `web_research` (`web-research-1`: web_search — keyless DuckDuckGo HTML parse or optional Tavily-compatible `SEARCH_API_URL`/`SEARCH_API_KEY` bundle; fetch_page — egress-gated 1 MB/15 s; research_brief — cited multi-component brief, never fabricates sources) and `summarizer` (`summarizer-1`: summarize_text/summarize_url/compare_documents, 24k-char cap with truncation notice, TL;DR/Key points/Quotes Tabs). Zero new dependencies; all HTTP via `shared.external_http`.
- **Adaptive UI designer** (`backend/orchestrator/ui_designer.py`, flag `FF_UI_DESIGNER` default on, budget `UI_DESIGNER_TIMEOUT_SECONDS` default 8 s PER PASS × `UI_DESIGNER_MAX_ROUNDS` default 3): rounds with ≥2 rich components get a bounded multi-round LLM conversation — pass 1 drafts an arrangement (layout tree of existing astralprims types whose leaves are `{"type":"ref","component_id":…}` references), later passes show the model its own current arrangement (structural sketch + JSON) and ask it to critique/improve or reply `DONE`; identical refinement = converged (`outcome=stable`), failed refinement keeps best-so-far, refinements must themselves reference every placed component — round AND canvas refs; omission repair runs only on draft passes (`outcome=rejected:incomplete` otherwise), unusable draft JSON gets format-retries with the failure fed back (pass-1 refusal/timeout/LLM-error falls back exactly like the legacy single pass). Tool output is NEVER rewritten; garnish components carry deterministic `dg_*` ids stamped once on the final arrangement. Layouts persist in the new `workspace_layout` table (overlay model — components keep their `saved_components` rows/identities; later layouts steal claimed refs; component removal prunes refs); `workspace_snapshot.layouts` captures designed state for the timeline. Delivery: components upsert first (identities assigned), then `_deliver_round_components` designs and pushes a full materialized-canvas `ui_render` (`_canvas_components` materializes refs PRE-ROTE; nested refs get `attributes["data-component-id"]` morph anchors — zero client changes); ANY designer failure falls back to the legacy `ui_upsert` flat append (fail-open, logged as `ui_designer.fallback{reason}`). Validators derive their palette from `webrender.allowed_primitive_types()` (the renderer registry — 31 types since the dashboard primitives) instead of hand-copied subsets. **Dashboard primitives** `badge`/`hero`/`keyvalue`/`timeline`/`rating`: classes in astralprims 0.2.0 (Astral-Primitives repo; publish = push main, version-gated), renderers in `webrender/renderer.py`, styles in `astral.css`, voice extraction in `rote/adapter.py`; agents may emit them as plain dicts until the 0.2.0 wheel is in the image (`requirements.txt` floats `>=0.1.0`). The connectors agent's `interactive_artifacts`/`claude_design` tools now emit REAL widgets (hero + metric/chart/table/timeline/keyvalue with sample data, extended input schema for caller-supplied series) instead of placeholder spec cards. FR-027: the constant "Analysis"/"Summary" cards are gone — `_chat_narrative` renders short answers as bare markdown and titles long ones from their own first heading.
- **CI** (`.github/workflows/ci.yml`): lint (ruff from repo root) / build (image artifact, GHA cache) / test (both pytest invocations INSIDE the built image vs postgres:17-alpine service, coverage.xml) / coverage-gate (diff-cover ≥90% on changed lines vs origin/main) / smoke (healthz+readyz dev boot; production-posture boot must exit EXACTLY 78 — run via `--entrypoint python orchestrator/orchestrator.py` because start.py supervises and would exit 0) / secret-scan (gitleaks + `.gitleaks.toml` allowlist) / publish (main only → ghcr.io `sha-<commit>` + `latest`). Deployment to https://sandbox.ai.uky.edu (Keycloak https://iam.ai.uky.edu) documented in docs/production-deployment.md (GHCR pull path). Constitution v2.1.0 added Principle XI codifying these gates.

## Chat attachments + universal parsing (feature 031)

- **Attach in chat**: the server-rendered chat input (`webrender/templates/shell.html` `#astral-form`) has a paperclip control + a `#astral-attachments` chips row; `serve_shell` injects the `accept` list from `attachments/content_type.ACCEPTED_EXTENSIONS` (single source). `client.js` stages files as chips (upload-per-file ≤10, remove, server-rejection + `parser_status` surfaced) and sends structured `chat_message.payload.attachments = [{attachment_id, filename, category}]` — the legacy `"[Attachment: …]"` text hack is retired (still tolerated for old transcripts). Paperclip opens a small menu (Upload / Choose from your files → the `attachments` chrome surface).
- **Delivery + parsing**: `Orchestrator.handle_chat_message(..., attachments=)` threads through `_serialized_chat`/`_dispatch_async_chat`; `_attach_turn_attachments` ownership-validates each id (drops foreign/invalid with a `file`-class audit), links `message_attachment` rows, and injects an **"Attachments on this turn"** block naming the reader tool (`parser_registry.coverage`) so the agent calls the right existing `read_*` tool. `load_chat` re-hydrates chips. Broadened allow-list adds many text/code formats (→ `read_text`) plus new no-parser `data`/`archive` categories (100 MB caps) that drive auto-creation; `Attachment.category` Literal + `AttachmentCategory` extended.
- **Eager safe auto-creation (US2)**: an uncovered upload returns `parser_status` (`covered|preparing|pending_admin_approval|unavailable`) and, when `FF_ATTACHMENT_AUTOPARSE` (default on), `attachment_autoparse.start` (background) drafts a `<EXT> Parser` agent by reusing the 027 lifecycle (create_draft → generate_code → start_draft_agent → `_self_test_draft(attachments=)` against the uploaded file, ≤1 auto-refine), dedup-keyed by format `gap_fingerprint` in the new `attachment_parser` table. Codegen SECURITY RULES now forbid assuming any pip install (stdlib + installed-only, best-effort extraction). Approval is **admin-only** (`agentic_creation._h_draft_approve` gates `auto_attachment` origin; uploader can't self-approve; refine/discard via `_decidable_draft`); go-live runs `_promote_parser_global` (`set_agent_visibility(True)` + read-scope grant + `attachment_parser.mark_live`) so the registry marks the format globally covered (fleet availability via the 030 public-catalog consent path). Audit: `agent_lifecycle` correlated by draft id (gap_detected→auto_created→self_test→approved/rejected). Fail-closed: an unapproved/failed parser yields "cannot read this type yet" — auto-generated code never runs outside the gate + self-test + admin approval.
- **Library (US3)**: `webrender/chrome/surfaces/attachments.py` (`attachments` surface) browses the user's uploads with Attach (client-side `astral-attach-existing` → stages a chip, no re-upload) + Delete (`chrome_attachment_delete` → `soft_delete` + blob removal).
- **Schema** (idempotent `_init_db` deltas): `message_attachment` (turn→attachment links), `attachment_parser` (format→global-parser registry, unique `gap_fingerprint`), guarded `draft_agents.source_attachment_id`. Flag `FF_ATTACHMENT_AUTOPARSE`. Zero new runtime deps.
- **Auto-continue on approval (T031)**: on admin approval `_promote_parser_global` calls `attachment_autoparse.auto_continue_after_go_live`, which recovers the uploader's original (un-augmented) request for the turn that carried the file and replays it in-process via a `VirtualWebSocket` — the now-live, scoped parser resolves the attachment to `covered`, the reader tool runs, and the parsed result persists into the original chat (user notified "I re-read your file"). Falls back to the notify ("ask again") if the original turn can't be recovered.

## In-process built-in agents, owner-safe marking, skills + slash commands (feature 040)

- **In-process built-ins (US1, `FF_INPROCESS_AGENTS` default on)**: the nine bundled first-party agents (connectors, dice_roller, general, journal_review, medical, ml_services, summarizer, weather, web_research) run IN-PROCESS in the orchestrator — `start.py` no longer spawns them as uvicorn subprocesses/ports. `orchestrator/local_agents.py` (`register_built_ins`, `BUILT_IN_AGENT_DIRS`) imports + instantiates each agent class (no `.run()`), registers it via the normal `register_agent(None, …)` path (card/tool-scope/security-flags/ownership/ECIES key), and stores it in `orch.local_agents`. Dispatch: `_dispatch_tool_call` + `execute_parallel_tools` branch on `agent_id in self.local_agents` → `_execute_in_process`, which runs the agent's REAL `handle_mcp_request` against a `shared/local_transport.py::LoopbackSocket` whose frames route back through `handle_agent_message` (same request-id↔future correlation, progress/stream routing, in-agent ECIES decrypt so the orchestrator never holds plaintext, `_runtime` injection). External A2A + draft subprocess paths untouched; the WS path is the kill-switch when the flag is off.
- **Owner-safe marking (US2, `FF_SAFE_AGENTS` default on)**: new `agent_trust` table (`is_safe/marked_by/marked_at/prior_state/revised_reset_at`, distinct from `is_public`). `orchestrator/agent_trust.py` (`mark_safe`/`reset_on_revision`/`seed_safe`, admin/owner-gated, audited `agent_lifecycle`). Boot seeds the nine built-ins safe; `apply_revision` resets the marker (re-approval). `tool_permissions.is_tool_allowed` flips the baseline deny→allow for a safe agent ONLY when the user has no explicit scope row — an explicit opt-out and hard security-flag blocks always win; computed at check time, no per-user rows written. Admin/owner UI on the agents chrome surface (`chrome_safe_set`).
- **etf_tracker_1 removed (US3)**: dir + `_FIRST_PARTY_PUBLIC_AGENT_IDS` + `_AGENT_ICONS` entry gone; `etf-tracker-1-1`/`etf_tracker_1` added to `RETIRED_AGENT_IDS` + `RETIRED_KNOWLEDGE_STEMS`; idempotent `_init_db` `_cleanup_retired_agents_040` purges orphaned permission/credential/ownership/trust rows.
- **Skill packs (US4, `FF_SKILL_PACKS` default on)**: authored, version-controlled packs under `backend/knowledge_packs/techniques/<slug>.md` (the synthesizer never writes there). `KnowledgeIndex.get_techniques_for_agent` reads authored-first (precedence over synthesized); `orchestrator/skill_packs.py::build_skill_digest` injects a bounded, relevance-scoped digest for ONLY the agents in play this turn into the system prompt (wires the previously-dormant loader); fail-open.
- **Slash commands (US5, `FF_SLASH_COMMANDS` default on)**: `orchestrator/slash_commands.py` curated set (`/help /agents /summarize /research /weather`). `handle_chat_message` expands a leading `/command` into a normal prompt (preserving the user's typed text as `display_message`) BEFORE processing, so the rewritten turn flows through the usual permission/audit/PHI gates — never a privileged bypass; unknown/malformed → friendly relay; a `/path`-style token is left as text. Discovery via a `#astral-slash-menu` typeahead in `shell.html`/`client.js`/`astral.css`.
- **Audit completeness (FR-032)**: `execute_parallel_tools` now dispatches through `_execute_with_retry_audited`, so parallel tool batches emit the same start/end `agent_tool_call` rows as the single-tool path. Zero new runtime deps; all schema via idempotent guarded `_init_db`.
<!-- MANUAL ADDITIONS END -->

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan at
- [specs/001-tool-stream-ui/plan.md](specs/001-tool-stream-ui/plan.md)
- [specs/002-file-uploads/plan.md](specs/002-file-uploads/plan.md)
- [specs/003-agent-audit-log/plan.md](specs/003-agent-audit-log/plan.md)
- [specs/004-component-feedback-loop/plan.md](specs/004-component-feedback-loop/plan.md)
- [specs/005-tooltips-tutorial/plan.md](specs/005-tooltips-tutorial/plan.md)
- [specs/006-user-llm-config/plan.md](specs/006-user-llm-config/plan.md)
- [specs/007-sidebar-settings-menu/plan.md](specs/007-sidebar-settings-menu/plan.md)
- [specs/008-llm-text-only-chat/plan.md](specs/008-llm-text-only-chat/plan.md)
- [specs/010-fix-page-flash/plan.md](specs/010-fix-page-flash/plan.md)
- [specs/011-nsf-grant-agent/plan.md](specs/011-nsf-grant-agent/plan.md)
- [specs/012-fix-agent-flows/plan.md](specs/012-fix-agent-flows/plan.md)
- [specs/013-agent-visibility-tool-picker/plan.md](specs/013-agent-visibility-tool-picker/plan.md)
- [specs/014-progress-notifications/plan.md](specs/014-progress-notifications/plan.md)
- [specs/015-external-ai-agents/plan.md](specs/015-external-ai-agents/plan.md)
- [specs/016-persistent-login/plan.md](specs/016-persistent-login/plan.md)
- [specs/025-agentic-soul-integration/plan.md](specs/025-agentic-soul-integration/plan.md)
- [specs/026-frontend-removal-astralprims/plan.md](specs/026-frontend-removal-astralprims/plan.md)
- [specs/027-agentic-creation-settings/plan.md](specs/027-agentic-creation-settings/plan.md)
- [specs/028-workspace-auth-revival/plan.md](specs/028-workspace-auth-revival/plan.md)
- [specs/029-agents-adaptive-ui-ci/plan.md](specs/029-agents-adaptive-ui-ci/plan.md)
- [specs/030-finish-soul-integration/plan.md](specs/030-finish-soul-integration/plan.md)
- [specs/031-attachment-upload-parsing/plan.md](specs/031-attachment-upload-parsing/plan.md)
- [specs/032-attachment-sdui-verification/plan.md](specs/032-attachment-sdui-verification/plan.md)
- [specs/033-frontier-techniques-research/plan.md](specs/033-frontier-techniques-research/plan.md)
- [specs/040-inprocess-agents-skills-commands/plan.md](specs/040-inprocess-agents-skills-commands/plan.md)
- [specs/041-android-sdui-client/plan.md](specs/041-android-sdui-client/plan.md)
- [specs/042-cross-client-chrome-parity/plan.md](specs/042-cross-client-chrome-parity/plan.md)
- [specs/043-sdui-settings-surfaces/plan.md](specs/043-sdui-settings-surfaces/plan.md)
- [specs/044-native-client-parity/plan.md](specs/044-native-client-parity/plan.md)
- [specs/045-thesis-framing-memo/plan.md](specs/045-thesis-framing-memo/plan.md)
- [specs/046-aip-differential-analysis/plan.md](specs/046-aip-differential-analysis/plan.md)
- [specs/047-security-benchmark-harness/plan.md](specs/047-security-benchmark-harness/plan.md)
- [specs/048-recursive-delegation-chains/plan.md](specs/048-recursive-delegation-chains/plan.md)
- [specs/049-ietf-id-decision/plan.md](specs/049-ietf-id-decision/plan.md)
- [specs/050-cresco-integration-decision/plan.md](specs/050-cresco-integration-decision/plan.md)
- [specs/051-apple-native-clients/plan.md](specs/051-apple-native-clients/plan.md)
- [specs/052-perf-comment-hygiene/plan.md](specs/052-perf-comment-hygiene/plan.md)
- [specs/053-apple-production-release/plan.md](specs/053-apple-production-release/plan.md)
- [specs/054-byo-llm-setup/plan.md](specs/054-byo-llm-setup/plan.md)
- [specs/063-remote-compute-agents/plan.md](specs/063-remote-compute-agents/plan.md)
- [specs/064-mcp-2026-07-28-decision/plan.md](specs/064-mcp-2026-07-28-decision/plan.md) *(decision verdicts ratified + a phased upgrade: Phase A envelope/dialect, Phase B a flag-gated conformant MCP endpoint)*
<!-- SPECKIT END -->
